# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# 虚拟环境（本项目历史上 .venv 指向失效路径，重建为 .venv_new，两者均不入版本库）
.venv/
.venv_new/
venv/
env/

# 测试与覆盖率
.pytest_cache/
.pytest_tmp/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml

# ── Node ────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.next/
.turbo/
out/
*.tsbuildinfo

# ── 环境变量与密钥 ──────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── 数据库与运行时产物 ──────────────────────────────────
*.db
*.sqlite
*.sqlite3
*.log

# ── 编辑器 / 操作系统 ───────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── 研究原始数据集（体积 13 GB 级，且受上游许可约束，不入版本库）──────
# data/ 存放四个公开数据集（assist09 / DBE-KT22 / XES3G5M / Junyi）的原始文件，
# 由 scripts/ 下的下载脚本按 docs/Kaggle与DataShop凭据获取指南.md 获取；
# 它们不是本项目的产物，且 Junyi 为 CC-BY-NC-SA-4.0（禁商用），故不随代码归档。
data/
data_backup/

# ── 外部对照系统的上游浅克隆（第三方代码，不属本项目产物）────────────
# results/m2/external/ 下是 M2 外部效度分析用的两个 Moodle 插件浅克隆：
#   DigiDago/moodle-format_ludilearn @ eb69582fb4def6e4cbb26c2146ceb90435951c53 (GPL-3.0)
#   FMCorz/moodle-block_xp          @ 65541fdc9c77511a906353f6660e195eeaa51893 (GPL-3.0)
# 体积 49 MB / 1432 文件，内容全是上游代码，且可按 results/M2_results.md 里记录的两行
# `git clone --depth 1` 原样重建，故不入本库——避免把他人的 GPL 代码当作本项目产物分发。
results/m2/external/

# ── 项目临时文件 ────────────────────────────────────────
tmp_*.json

# 注：artifacts/ 目录刻意**不**忽略——它是论文可复现性印章的存放处，
#     需要随代码一起归档（Zenodo 归档时会连同提交哈希一起冻结）。
# !artifacts/

# 例外：artifacts/state/ 是 LEARNFLOW_STATE_BACKEND=json 时的运行时状态落盘目录，
# 存放的是被试的实时游戏化状态（连胜、宝箱进度、每日挑战等）——属于**研究原始数据**，
# 受伦理与数据最小化约束，绝不可随代码归档或推送到公开仓库。
learnflow-backend/artifacts/state/

# 同理：LEARNFLOW_EXPERIMENT_BACKEND=json 时的实验落盘文件，
# 含被试分组信息（谁被分到 control/treatment），同样属研究原始数据，不可归档。
learnflow-backend/artifacts/experiments.json
