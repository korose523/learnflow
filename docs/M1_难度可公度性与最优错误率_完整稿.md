# 难度量纲的可公度性：一个不稳定性命题及其在四个真实学习系统上的检验

## ——兼论最优错误率的描述性证据与因果识别的未竟之处

**English Title:** Commensurability of Difficulty Scales: An Instability Proposition and Its Tests on Four Real Learning Systems — With Descriptive Evidence on the Optimal Error Rate and the Unfinished Causal Identification

> **目标中文期刊**：《开放教育研究》（CSSCI、北大核心、AMI(A刊)核心；收稿范围含"学习分析与教育大数据"；不收版面费与审稿费，不强制基金）。
> **备选期刊**：《数据分析与知识发现》（CSSCI、北大核心、CSCD(扩展)、EI；征稿范围含"数字教育""教育数据集与智能校园"）。
> **学科外壳**：学习分析 / 教育测量（测量侧的公制问题 + 一组判据依赖性负结果）。
> **本文性质**：**已完成的研究**，但只在测量侧完成；因果侧（以事前错误率为剂量的剂量—反应识别）**未执行**，本文仅报告一个描述性分布证据，并明确不作因果主张（详见 1.3 节与第 8 节）。
> **数据与代码可得性**：本文全部数字由 `results/code/` 下的脚本实际运行产出，原始 JSON 产物随稿；运行命令见各节脚注与附录式说明。被测系统为 `E:\learnflow`（K12 自适应学习平台，Zenodo 归档 DOI 10.5281/zenodo.22719229），本文的实测**未修改**该仓库任何文件，三处代码改动（第 9.2 节）发生在本文写作之前，其中前两处已有回归测试保护。

---

## 文首声明（必读）

**一、本文报告已完成的结果，但不是一项完整的研究。** 测量侧（难度量纲的可公度性、融合公制的不变性、多信号融合的增益及其边界）已由合成数据与四项真实数据集上的实测完成；因果侧（以事前错误率为剂量、以后续掌握速度为结局的离策略剂量—反应估计，以及 85% 规则的五前提分解）**没有执行**，原预注册中的假设 H4–H7 因此处于未检验状态。本文在第 8 节给出一个描述性的滑窗错误率分布证据，并明确声明它不构成对 85% 规则的检验。凡涉及因果的表述，本文一律使用"描述性""相关""未识别"措辞。

**二、数字纪律声明。** 本文涉及研究依托系统的规模与机制计数时，一律采用可复算脚本的权威口径——游戏化干预机制 **54 个**（LF-M01…LF-M54，连续无缺口）、学习方法 **28 种**、机制注册表指纹 `ee1a49be5732`；不出现任何虚高的机制计数。被测系统后端全量测试在本文所引代码改动后为 **934 条通过**（改动前为 920 条，新增 14 条为难度公制层的回归测试）；凡引用测试数量，本文一律写 934。

**三、数字口径更正记录（该数值不得作为结果引用）。** 早期一批漂移实验把三个难度源的符号约定混用（IRT 源取 logit(正确率)，正方向为"易"；时长与尝试次数为原始值，正方向为"难"），由此得到的线性融合漂移被系统性抬高。统一为"正方向 = 难"后重算，线性 min-max 融合在 assist09 上的真实漂移为 **L1 = 0.5357**。本文所有结论一律采用 0.5357 口径；被更正的旧数值只在本条记录中以"更高"定性出现，不写出具体数字，也不得被引用为结果。

---

## 摘要

自适应学习系统每天都要回答"下一题给什么难度"，但"难度"是否是一个可以被放在同一把尺子上度量的量，这一问题被系统性地跳过了。本文把该问题改写为一个可证伪的测量学命题并给出三项结果。**第一，不稳定性命题（C1）成立。** 若把各难度源先各自归一化到 [0,1] 再线性加权，则以边际贡献定义的"有效权重"既不等于名义权重（合成实验中 π₁ = 0.532 ± 0.026 对名义 w₁ = 0.40），也会随任一源的**单调重参数化**而漂移：仿射变换下漂移精确为 0（min-max 吸收），而非线性单调变换下 L1 均值 0.1276（SD 0.0196）、36.4% 的（源, 变换）组合出现权重排序反转，最强反例中权重向量完全反号（Spearman −0.20，L1 = 0.6893）。改用经验 CDF → logit 的分位公制后，同校准样本下漂移严格为 0、独立校准样本下仅 0.016 的链接抽样噪声、零排序反转。在真实数据（assist09，994 题）上，线性 min-max 融合的漂移 L1 = 0.5357、排序反转显著，分位公制下降至 0.0001 且反转归零。**第二，多信号融合的增益是"与专家标签的一致性增益"，不是预测力提升。** 在 DBE-KT22（212 题）上，专家难度与经验难度的秩相关仅 0.2207，换用联合 IRT-1PL 只提到 0.2300；把五个行为信号以分位公制等权融合后升至 0.2899，优化权重的重复 5 折 ×10 交叉验证为 0.5240 ± 0.1069，其驱动力是一个"学生自报难度高且信任度低"的**差分特征**。在 Junyi（1,234 练习、1,621 万条交互、无自陈反馈）上同向复现：0.2610 → 0.3629，重复交叉验证 0.4023 ± 0.0480；bootstrap 配对增益 Junyi Δ = +0.1017 [0.0726, 0.1325]（p = 0.000），DBE Δ = +0.0723 [−0.0195, 0.1649]（单侧 p = 0.059，仅边缘显著）。**第三，上述增益在换判据后反转。** 专家标签与真实表现难度的秩相关只有 0.4326；把判据换成"未见过样本的真实表现"后，等权融合在 Junyi 与 DBE 的**全部**样本量档上均劣于仅用成功率（Junyi −0.0267 → −0.1222，DBE −0.1752 → −0.2113，融合胜率 0.00）。定位到的原因之一是融合配置中的一处**方向性错误**（升级率被赋予正权重），剔除并引入样本量自适应收缩 λ\*(k) = 1/(1+(k/27.4)^0.902)（R² = 0.9989；部署值取留一 k 交叉验证拟合的 27.3 / 0.895）后，小样本档首次反超（k = 10 时 +3.43 个百分点），但收益在 k ≥ 100 时降至 0.22 个百分点以下。进一步地，本文对融合估计器本身作了两项优化——**按拟合段内秩相关对每个信号作方向校正（保留反向信号的信息，而非整列剔除）**，并**以逐信号折半信度加权**——得到 `srw7` 估计器；在记录级与学生级双重留出验证下，它在全部样本量档上均不劣于"仅成功率"基线，且在所有档上高于 O8 的 fused6（记录级 +3.823 / +2.025 / +0.930 / +0.391 / +0.138 pp；学生级 +4.283 / +2.189 / +0.951 / +0.335 / +0.102 pp），并在学生级上消除了 fused6 在 k ≥ 25 时低于基线的有害性。本文最后报告 assist09 上的滑窗错误率分布（均值 0.3511，**众数区间 0.15–0.20**，即成功率 80–85%），并明确说明这只是描述性证据：它与 85% 规则的 15.87% 及先行研究的 80.7% 均相容，但分布众数的宽度不足以在两者之间作出判别。

**关键词**：难度测量；可公度性；不稳定性命题；分位链接；专家标注；判据依赖性；最优错误率；负结果

## Abstract

Adaptive learning systems must answer "what difficulty should the next item have?" every day, yet the prior question — whether difficulty is a quantity that can be placed on one common scale — is systematically skipped. We reformulate that question as a falsifiable measurement proposition and report three results. **First, the instability proposition (C1) holds.** If difficulty sources are each normalised to [0,1] and then linearly combined, the "effective weights" defined by marginal contribution equal neither the nominal weights (π₁ = 0.532 ± 0.026 against nominal w₁ = 0.40 in simulation) nor themselves under a **monotone reparameterisation** of any single source: drift is exactly zero under affine transforms (absorbed by min-max) but averages L1 = 0.1276 (SD 0.0196) under nonlinear monotone transforms, with rank reversals in 36.4% of (source, transform) cells and complete sign reversal in the strongest counterexample (Spearman −0.20, L1 = 0.6893). An empirical-CDF-to-logit quantile metric gives exactly zero drift under shared calibration, 0.016 linking sampling noise under independent calibration, and zero reversals. On real data (assist09, 994 items), linear min-max fusion drifts by L1 = 0.5357 with substantial reversals, while the quantile metric reduces this to 0.0001 with reversals eliminated. **Second, the gain from multi-signal fusion is a gain in consistency with expert labels, not in predictive power.** On DBE-KT22 (212 items), the rank correlation between expert and empirical difficulty is only 0.2207; a joint IRT-1PL estimator raises it merely to 0.2300, whereas equal-weight fusion of five behavioural signals on the quantile metric reaches 0.2899, and optimised weights reach 0.5240 ± 0.1069 under repeated 5-fold × 10 cross-validation — driven by a **differential feature** combining high self-reported difficulty with low confidence. Junyi (1,234 exercises, 16.2 M interactions, no self-report channel) replicates this: 0.2610 → 0.3629, repeated CV 0.4023 ± 0.0480; paired bootstrap gains are Δ = +0.1017 [0.0726, 0.1325] (p = 0.000) for Junyi and Δ = +0.0723 [−0.0195, 0.1649] (one-sided p = 0.059) for DBE. **Third, these gains reverse when the criterion changes.** Expert labels correlate only 0.4326 with real performance difficulty; when the criterion becomes "true performance on unseen samples", equal-weight fusion loses to success rate alone at **every** sample size on both datasets (Junyi −0.0267 → −0.1222; DBE −0.1752 → −0.2113; fusion win rate 0.00). One identified cause is a **sign error** in the fusion configuration; removing it and adding sample-size-adaptive shrinkage λ\*(k) = 1/(1+(k/27.4)^0.902) (R² = 0.9989) makes fusion win for the first time at small samples (+3.43 pp at k = 10), though the gain falls below 0.22 pp for k ≥ 100. We finally report the sliding-window error-rate distribution on assist09 (mean 0.3511, **modal bin 0.15–0.20**, i.e. 80–85% success) and state plainly that this is descriptive: it is compatible with both the 15.87% rule and the prior literature's 80.7%, and the modal bin is too coarse to discriminate between them.

**Keywords**: difficulty measurement; commensurability; instability proposition; quantile linking; expert labels; criterion dependence; optimal error rate; negative results

---

## 1 引言

### 1.1 一个被跳过的问题

没有一个自适应学习系统可以回避"下一题给什么难度"。工程上的通行做法是：把若干被称为"难度"或"能力"的量——知识追踪的掌握概率、间隔重复的记忆难度、竞技评分的能力估计、最近若干次作答的窗口成功率——各自归一化，然后放进一个带硬编码权重的加权和里。这一做法预设了这些量已经处在可比较的尺度之上。而这个预设，据本文所及的文献，从未被单独检验过。

这个预设之所以可疑，不是因为归一化做得不够仔细，而是因为它在测量论的意义上**没有定义**。加权和这一运算要求各加数同属一个可以作线性组合的量纲类型：顺序量级、区间量级与比率量级之间不存在良定义的加法。当一个系统把"掌握概率"与"记忆难度"相加时，它实际上做了一次未经声明的公制选择，而这次选择的后果会被权重吸收、进而被误读为"难度配方"的效果。

本文把这一现象命名为**不稳定性**，并把它写成可证伪的命题：在把各源归一化到 [0,1] 后作线性加权时，以边际贡献定义的有效权重会随任一源的单调重参数化而漂移；而若各源先经经验 CDF 链接到 logit 公制再融合，则有效权重对任意严格单调重参数化**严格不变**。这一对比不是"我们做了归一化"式的工程常识，而是关于"当前主流实现为何在测量论上不成立"的一个可计算断言。

### 1.2 为什么这不是技术细节

有人会问：权重漂移几分，有多大关系？关系在于它决定了系统的可解释性与可调性。若"0.45 / 0.35 / 0.20"这组权重在把知识追踪侧的实现由 `mastery × 9 + 1` 改为 `mastery` 之后就变成了另一组数，那么围绕这组权重做的一切经验调参、一切 A/B 结论、一切"我们发现掌握度应占更大比重"的说法，都只是在描述一次特定的实现选择，而不是描述难度本身。可公度性问题因此不是前置的技术清理，而是后续一切难度研究能否被解释的前提。

本文的写作动机正是这种前提性。原预注册设计把这层关系表述为"测量目标是因果目标的前提：若剂量本身不是良定义的难度量，其因果效应也无从解释"。这句话在本文仍然成立，但它的实践后果比预想更直接：**测量侧的检验完成了，因果侧没有完成**，因而本文是一份在测量侧给出完整结果、在因果侧给出诚实缺口的稿件。这个不对称本身是信息，本文不掩饰它。

### 1.3 本文不主张什么

为避免读者按惯例从标题推断贡献，这里先列出四条"不主张"：

**不主张**多信号融合提升了难度估计的预测力。第 7 节将给出决定性证据：在留出验证下，融合反而劣于仅用成功率。第 6 节报告的全部增益，其判据是**专家难度标签**，因此对它的正确表述是"与专家标签的一致性增益"。

**不主张**最优错误率等于或不等于 15.87%。第 8 节只报告一个描述性的滑窗错误率分布；原设计的双重稳健（AIPW/DR）离策略评估、剂量—反应曲线拟合、非单调性检验与五前提分解**均未执行**，故本文不作任何关于最优错误率点估计或置信区间的断言。

**不主张**知识树结构可以作为难度代理。第 5.5 节给出一条干净的小型负结果：知识树深度与经验难度的秩相关只有 +0.0785 且分层均值非单调，父子边上的难度梯度一致性为 0.487（约等于抛硬币）。

**不主张**本文的被测系统在难度处理上是特例。恰恰相反：本文所用的被测系统是一个真实建成、已公开归档的 K12 自适应学习平台（54 个游戏化干预机制、28 种学习方法、注册表指纹 `ee1a49be5732`），其难度融合的具体写法与同类系统的通行写法并无二致。本文批评的是一类实现，不是一个实现。

### 1.4 本文的贡献与主要发现

**贡献一：把"难度可公度性"从一个设计词汇改造为可计算、可证伪的命题，并在合成与真实数据上双重验证。** 合成实验（第 4 节）给出命题成立的完整数值图景：线性 [0,1] 公制下仿射漂移精确为 0、非线性单调漂移 L1 = 0.1276、36.4% 组合排序反转、最强反例完全反号；分位 logit 公制下同校准漂移为 0、独立校准仅 0.016 抽样噪声。真实数据（第 5.3 节）给出工程落地证据：assist09 上线性融合 L1 = 0.5357，换用分位公制后降至 0.0001，排序反转归零。

**贡献二：给出"专家标注—行为证据"两套难度证据之间的距离，并据此重估多信号融合的价值。** DBE-KT22 上专家难度与经验难度秩相关仅 0.2207（p = 0.0012），换用联合 IRT-1PL 只提升到 0.2300——瓶颈不在估计器；但把五个行为信号以分位公制等权融合可升至 0.2899，优化权重在重复 5 折 ×10 交叉验证下达 0.5240 ± 0.1069。其机制被定位到一个差分特征：自报难度与信任度在题级 Pearson 相关高达 +0.94，而在分位空间中"信任度反向"与自报难度的相关为 −0.80，二者相减恰好消掉共同模态噪声。

**贡献三：以判据切换的方式给出一组负结果，并定位到一处实打实的方向性错误。** 专家标签与真实表现难度的秩相关仅 0.4326（第 7.1 节）；把判据换成"未见过样本的真实表现"后，等权融合在 Junyi 与 DBE 的**全部**样本量档上均劣于仅用成功率，融合胜率 0.00（第 7.2 节）。进一步定位到：升级率与专家标签的相关为 −0.1837、在池内为 −0.756，却被优化器赋予了 +0.154 的**正权重**；剔除该信号并引入样本量自适应收缩后，小样本档首次反超（k = 10 时 +3.43 个百分点），但收益随样本量迅速衰减至 k ≥ 100 时的 0.22 个百分点以下；"融合增益"的正确表述是与专家标签的一致性增益，而非预测力提升（第 7.2 节）。记录级留出的效度缺口另由按学生切分的留出验证补齐，结论不变（第 7.6 节）。在此基础上，第 7.8 节报告融合估计器的两项优化：把方向错误的信号由"整列剔除"改为"符号校正"，并加入逐信号折半信度加权（`srw7`）。在记录级与学生级双重留出下，`srw7` 均在全部样本量档上不劣于"仅成功率"基线，并在所有档上高于 fused6；学生级上 `srw7` 相对 fused6 的配对胜率为 0.902–1.000，Wilcoxon 符号秩 p ≤ 1.29 × 10⁻⁶⁶。该结果同时表明，O8 的"剔除升级率"虽方向正确，却丢弃了该信号的有效信息，并非最优处置。

**贡献四：在跨系统尺度上界定上述结论的适用范围。** 四项数据集覆盖了三种不同的证据制度：DBE-KT22 有专家标注且有学生自陈反馈；Junyi 有专家标注但无反馈；XES3G5M 有完整题干与知识树但无专家难度；assist09 有提示与多技能标注。融合增益在 DBE 与 Junyi 上同量级复现（+31% 与 +39%），说明这不是单一系统的特性；而 λ 收缩的收益只在 Junyi 上得到验证，DBE 上无收益——本文如实报告这一未复现。

### 1.5 结构与可复现性

第 2 节梳理相关工作；第 3 节给出难度源的类型学与不稳定性命题的形式化；第 4 节报告合成数据上的命题验证；第 5 节报告真实数据上的公制修复与两条负结果；第 6 节报告多信号融合在专家标签判据下的增益；第 7 节切换判据并报告结论反转、方向性错误、样本量自适应收缩，以及该收缩公式的留一 k 交叉验证（区间内插）检验与学生级留出验证；第 8 节报告最优错误率的描述性证据与因果识别的未竟之处；第 9 节讨论含义、代码落地与局限；第 10 节列出诚实缺口清单；第 11 节结论。

**可复现性说明。** 本文全部数字由下列脚本实际运行产出，确定性无随机种子依赖或种子已固定：`results/code/m1_instability.py`（合成，种子 `902000+r`）、`m1_real_data.py`、`optimized_experiments.py`、`o4_fusion.py`、`o4b_fusion.py`、`o5_junyi_fusion.py`、`build_difficulty_cache.py`、`o6_difficulty_stats.py`、`o7_holdout_validation.py`、`o8_fusion_variants.py`、`o9_lambda_generalization.py`、`o10_user_holdout.py`。原始产物为同名 `.json`。本文不复算、不重估、不补齐任何未执行的实验。

---

## 2 相关工作

### 2.1 知识追踪与难度—能力的混淆

深度知识追踪是预测"学生在下一题上答对概率"的主流范式，但它的输出是**概率**，不是难度。当系统把这一概率当作难度信号使用时，难度与能力的混淆便发生了：高正确率既可能来自题目容易，也可能来自学生能力强。这一混淆在文献中的表现之一，是不同研究在同一套不可比的尺度上比较 AUC。Liu 等（2025）在标准化 21 个深度知识追踪模型与 9 个数据集后指出，该方向"方法高度相似、结论分歧极小、AUC 报告不一致"——分歧的来源之一正是被比较的量本身不在同一公制上。本文的处理方式不是提出又一个预测模型，而是把"这些量是否可加"单独提出来检验，这与该综述所指出的标准化缺口是同一问题的两面。

### 2.2 测试等值与 IRT 链接

等值文献解决的是"不同测验形式能否可比"，其技术目标与本文的"不同难度技术路线能否可比"共享同一测量论根基。Weber、Becker、Spinath 与 Koch（2025）以 400 个仿真情境系统比较五种等值方法（MS、MM、MGM、IRF、TRF）在 2PL/3PL 下的参数复原，结论对本文有直接的方法学含义：**等值方法的选择影响很小，真正决定复原质量的是锚题的高区分度、低猜对概率与大样本**；样本量低于 100 时参数复原不可接受。这一结论支持本文的做法——把资源投向公制与信号质量，而非投向更复杂的链接方法。Deng（2025）以 PISA 2018 科学模块的阿拉伯语与中文组数据检验快速猜测对多组并发 IRT 标定的影响，发现该程序对锚题识别与能力估计稳健，但会降低个体能力估计的精度。两项研究共同界定了本文标定设计的边界：快速猜测对**难度链接**的污染是否可测，仍是一个开放问题，本文不作断言。

### 2.3 难度估计的多信号化与专家标注的位置

自适应排序真正需要的"难度"，长期被默认为可由正确率单一信号估计。Ding 等（2024）的 Easy2Hard-Bench 以 IRT 与 Glicko-2 联合标定为标准化的 LLM 难度评测提供难度标签，代表了"把难度当作可标定参数"的主流路线。本文的分位公制与该路线共享 IRT 的 logit 直觉，但把对象从"题目答对率"换成"任意单调观测的秩"，从而把不变性从估计问题提升为解析恒等式。需要强调本文与这些工作的分工差异：它们追求更准的难度标签，本文追问的是**这些标签处在哪个判据之下**。第 7 节将表明，这一追问会改变结论的符号。

### 2.4 85% 规则、挑战点框架与真实学习者上的证据

Wilson 等（2019）在梯度下降类二分类学习者上推导出最优错误率 15.87%，即"85% 规则"。这是一条关于特定学习算法的定理，其推导依赖五项前提：二分类任务、无提示、无跳过、无遗忘、无迁移。真实课堂在五个维度上同时偏离。值得注意的是，这一适用性缺口并非由后来的批评者从文献缝隙中挖掘，而是由原作者公开标示：Wilson 等明确提示该规则"可能不会直接适用于学生，因为他们并不是在学习二分类任务"。理论边界还不止于此——当噪声分布由高斯改为拉普拉斯或柯西，最优准确率分别漂移至约 82% 与 75%；对具有完美记忆的贝叶斯学习者，并不存在这样的甜点区。

真实学习者上的方向性证据来自 Baillifard、Belardi 与 Martarelli（2025）：在一门数字技能课程的 413 名学习者日志上，绩效在约 80.7% 成功率处达到最优，且最优策略在最坚持、最高绩效的学习者之间存在显著异质性。Guadagnoli 与 Lee（2004）的挑战点框架（CPF）及其扩展（Hodges 与 Lohse，2022）提供了机制性解释：CPF 把最优难度从"单点"重构为"功能性任务难度 = f(名义难度, 学习者技能, 练习条件)"的动态区间，并显式纳入动机成本，因此**真实最优点理应低于 85%**。本文第 8 节的分布证据与这一预期相容，但由于原设计的因果识别未执行，本文不宣称检验了这一预期。

### 2.5 离策略评估：本文计划过但未执行的部分

自适应学习系统几乎从不进行在线随机对照实验，学生的选题由系统策略与自选择共同决定，因此"若把该学生置于另一难度档，其掌握速度会如何"是一个反事实问题。离策略评估提供了标准工具：以倾向模型刻画选择概率、以结果模型刻画结局，再用逆概率加权或双重稳健估计量给出反事实均值（Dudík、Langford 与 Li，2011）；带交叉拟合的双重机器学习进一步降低了结果模型的正则化偏误（Chernozhukov 等，2018）；E-value 则用于量化未观测混淆需要多强才能解释掉估计效应（VanderWeele 与 Ding，2017）。这套工具箱在教育数据挖掘中仍属稀缺，也是原预注册设计的核心方法。

**必须说明：本节所列方法在本文中均未被使用。** 原设计的剂量（前向标定的事前错误率）、结局（掌握速度）与 AIPW/DR 估计器没有被执行，第 8.3 节给出原因与后果。本文保留本节，是为了让读者能够判断"本文缺了什么"，而不是为了让读者误以为本文做了因果推断。

---

## 3 理论：不稳定性命题

### 3.1 四个难度源与它们的量纲类型

设系统持有四个源：知识追踪掌握度 m ∈ [0,1]、间隔重复记忆难度 D、竞技评分能力 θ_EL、窗口成功率 r ∈ [0,1]。按经典量表分类与难度测量的三平面分类（construction / scale / dependence），四者分属不同类型：

| 源 | 量纲类型 | construction | scale | dependence |
|---|---|---|---|---|
| KT 掌握度 m | 有界潜变量（整数化后近似顺序量级） | 机器推断 | 静态、连续 | 依赖真实作答 |
| SR 记忆难度 D | 顺序量级 | 机器 + 人 | 动态、离散 | 模型特定 |
| EL 竞技评分 θ_EL | 区间量级（对数几率尺度） | 机器 | 动态、连续 | 不直接依赖真值 |
| WR 窗口成功率 r | 比率量级 | 机器 | 静态、连续 | 依赖真实作答 |

这里有两个关键判断。其一，**窗口成功率不是难度，而是难度与能力的联合结果**：它近似满足 r ≈ σ(θ − β)，同时编码了能力与难度两个量，把它与纯粹的难度量线性相加是一个范畴错误。其二，四者的量纲类型不一致——顺序量级、比率量级与区间量级之间不存在良定义的线性组合。这两点共同构成不稳定性命题的直觉来源：不是融合权重没调好，而是被加的对象不属于同一个可加的空间。

### 3.2 两种公制与它们的不变性

设融合规则为 S = Σ_k w_k · z_k，其中 z_k 是第 k 个源经某种映射后的值，w_k 为名义权重。以边际贡献法定义**有效权重**：

  **π_k = w_k · Cov(z_k, S) / Var(S)**，且 Σ_k π_k = 1。

当各源正交且离散度相等时 π = w；否则 π 依赖各源的离散度与相关结构——这正是漂移的来源。

本文比较两种公制：

| 公制 | 定义 | 对重参数化的不变性 |
|---|---|---|
| A：线性 [0,1] | 逐列 min-max 后线性加权 | **仅对仿射 y′ = ay + b 不变**（min-max 吸收）；非线性单调变换下漂移 |
| B：分位 logit | 逐列以阶跃经验 CDF 链接到 logit 潜尺度 ψ = ln(p/(1−p))，p = (#{x_cal ≤ x} + 0.5)/(n + 1) | **对任意严格单调变换严格不变**（秩不变性，解析恒等） |

公制 B 的不变性是一个恒等式而非发现：对严格增的 g，有 #{g(x_cal) ≤ g(x)} = #{x_cal ≤ x}，故 ψ 逐点不变。本文把它作为**验证对象**而非贡献：用有限样本数值复算，确认偏差为 0，并额外报告独立校准样本下的抽样噪声量级。

### 3.3 命题 C1 与其可检验含义

**命题 C1（不稳定性）.** 若各源先各自归一化到 [0,1] 再线性加权，则有效权重 π 一般不等于名义权重 w，且会随任一源的单调重参数化而漂移；若各源先经经验 CDF 链接到 logit 公制再融合，则 π 对任意严格单调重参数化严格不变，且在有限校准样本下的偏差仅来自链接抽样误差。

由此得到三条可检验含义，它们分别在第 4 节（合成）与第 5 节（真实）中被检验：

- **P1**：公制 A 下，基准（未变换）的有效权重已偏离名义权重；
- **P2**：公制 A 下，仿射重参数化漂移为 0，非线性单调重参数化漂移 > 0，且存在使排序反转的变换；
- **P3**：公制 B 下，同校准漂移为 0，独立校准下漂移仅为抽样噪声量级，且零排序反转。

---

## 4 方法验证：合成数据上的命题检验

### 4.1 生成式与实验设置

本节的目的是在**已知真值结构**的环境中检验命题，而不是估计任何真实系统的参数。共同潜能力 θ ∼ N(0,1)，样本量 n = 500，校准样本另行独立抽取 n_cal = 500；四个源为 θ 的单调函数加源特异噪声：

| 源 | 生成式 | 尺度 |
|---|---|---|
| X₁ 掌握度 | σ(1.1θ + 0.2 + 0.30ε₁) | 概率 [0,1] |
| X₂ 记忆难度 | exp(0.7θ + 0.3 + 0.25ε₂) | 正尺度（指数型） |
| X₃ 竞技评分 | 1200 + 250θ + 40ε₃ | 宽线性尺度 |
| X₄ 窗口成功率 | σ(0.9θ − 0.1 + 0.30ε₄) | 概率 [0,1] |

ε_k ∼ N(0,1) 独立。四源是同一潜能力的相关单调观测——即"真实共同公制存在"这一最有利环境——但各自离散度与形状不同。名义权重主配置为 w = (0.40, 0.30, 0.20, 0.10)，稳健配置为三源 w = (0.45, 0.35, 0.20)。对任一单源施加 9 种仿射变换（a ∈ {0.5, 1, 2} × b ∈ {−0.5, 0, 0.5}）与 7 种非线性单调变换（`power2`、`power3`、`sqrt`、`log`、`exp_scaled`、`probit_std`、`logit_minmax`）；变换参数在测试样本上固定后再同参数施加于校准样本，以保证是同一个重参数化。每种（源, 变换）重复 R = 200 次。

### 4.2 基准：有效权重已经不等于名义权重

| 公制 | π₁ | π₂ | π₃ | π₄ |
|---|---|---|---|---|
| 名义权重 w | 0.400 | 0.300 | 0.200 | 0.100 |
| **线性 [0,1]**（均值 ± SD） | **0.532 ± 0.026** | 0.171 ± 0.032 | 0.179 ± 0.009 | 0.119 ± 0.006 |
| **分位 logit** | 0.403 ± 0.006 | 0.299 ± 0.006 | 0.201 ± 0.003 | 0.097 ± 0.003 |

**可检验含义 P1 成立。** 线性 [0,1] 融合下，"0.40 / 0.30 / 0.20 / 0.10"这组权重**名不副实**：由于掌握度源的相对离散度最大，其有效权重被系统性放大到 0.532，而名义上占 0.30 的记忆难度源实际只贡献 0.171。分位 logit 公制下有效权重回到名义值附近（最大偏差 0.003），这说明该系统在此设置下确实存在唯一的可公度刻度，而 min-max 归一化恰好没有落在它上面。

### 4.3 公制 A：仿射不变、非线性单调漂移

| 变换族 | L1 均值 | L1 SD | 平均排序反转 | 出现反转比例 | 与基准权重的 Spearman |
|---|---|---|---|---|---|
| 仿射（9 种） | **0.000000** | 0.000000 | 0.000 | 0.000 | 1.0000 |
| 非线性单调（7 种） | **0.127620** | 0.019553 | 0.513 | **0.364** | 0.8646 |

**可检验含义 P2 成立，且是分两半成立的。** 仿射变换下漂移精确为 0——这是 min-max 归一化的解析性质，也是线性公制**唯一**的不变性；非线性单调变换下 L1 均值 0.1276，最大单格 0.674，36.4% 的（源, 变换）组合出现权重排序反转。这一分裂具有直接工程含义：真实系统中掌握度、记忆难度、竞技评分、成功率恰恰来自概率尺度、指数尺度、宽线性尺度与比率尺度，它们之间的转换**从来不是仿射的**，因此线性融合在实践中的稳定性恰好落在被证伪的那一半。

逐源最严重的变换如下（L1 均值 / 95% 区间 / 平均反转次数）：

| 源 | 最严重变换 | L1 均值 | 95% 区间 | 平均反转 |
|---|---|---|---|---|
| X₁ 掌握度 | `logit_minmax` | 0.674 | [0.641, 0.709] | 2.835 |
| X₁ 掌握度 | `log` | 0.193 | [0.116, 0.325] | 0.065 |
| X₂ 记忆难度 | `probit_std` | 0.368 | [0.274, 0.487] | 0.670 |
| X₂ 记忆难度 | `exp_scaled` | 0.286 | [0.163, 0.343] | 1.330 |
| X₃ 竞技评分 | `logit_minmax` | 0.274 | [0.248, 0.298] | 1.590 |

值得注意的是最严重的两格都发生在 `logit_minmax` 上——即"把源先 min-max 再取 logit"这一看起来最自然的改造，反而造成最大的权重漂移。这提示一个反直觉的实践结论：在公制未统一之前，任何"把某个源做得更像 logit"的局部改良都可能放大而非减小系统的整体不稳定性。

### 4.4 公制 B：同校准零漂移，独立校准仅抽样噪声

| 变体 | 变换族 | L1 均值 | 平均排序反转 | 出现反转比例 |
|---|---|---|---|---|
| 同校准 · 同变换 | 全部 | **0.000000** | 0.000 | 0.000 |
| 独立校准样本 | 仿射 | 0.0160 | 0.000 | 0.000 |
| 独立校准样本 | 非线性 | 0.0159 | 0.000 | 0.000 |

**可检验含义 P3 成立。** 同校准下漂移严格为 0（数值上全表最大 |Δπ| = 2.7 × 10⁻³，仅出现在 `logit_minmax` 一格，系定义域边界 clip 制造至多两个并列值的数值伪影，非概念性漂移）。独立校准下 0.016 量级的偏差对仿射（0.0160）与非线性（0.0159）**完全一致**，证明它与重参数化无关，而是有限校准样本的链接抽样误差。零排序反转在两种变体下都成立。

### 4.5 三个反例：信息量未变，权重排序反转

**反例 1（最强）**：对 X₁ 掌握度施加 `logit_minmax`（严格单调），第 0 号重复的结果为

| 项 | 值 |
|---|---|
| 基准有效权重 π⁽⁰⁾ | [0.5429, 0.1514, 0.1831, 0.1226]，排序 [1, 3, 2, 4] |
| 变换后 π⁽ᵍ⁾ | [0.1983, 0.2840, 0.3110, 0.2068]，排序 [4, 2, 1, 3] |
| L1 | **0.6893** |
| 排序反转次数 | **3**（完全反转） |
| 与基准权重的 Spearman | **−0.20** |
| 同一变换下分位 logit 公制 \|Δπ\| | **0.000**（无反转） |

**反例 2**：对 X₂ 记忆难度施加 `probit_std`，π 由 [0.543, 0.151, 0.183, 0.123] 变为 [0.415, 0.352, 0.140, 0.094]，L1 = 0.400，反转 1 次；分位公制 |Δπ| = 0。

**反例 3**：对 X₂ 施加 `power3`，π → [0.611, 0.047, 0.205, 0.137]，L1 = 0.209，反转 1 次；分位公制 |Δπ| = 0。

三个反例所用的变换**全部严格单调**，且 θ 未变——即"信息量完全不变、仅仅换了刻度"的情况下，线性融合的权重排序发生了实质反转，最强一例完全反号。这是本文对"权重漂移只是几分的小事"这一质疑的正面回应：漂移可以大到把权重的解释完全颠倒。

### 4.6 合成实验的边界（诚实声明）

**本节全部为合成数据**，生成方式已完全写明，未使用任何外部数据。三点边界必须声明：其一，四源共享单一潜变量是"可公度性"命题的**最有利**环境——即便如此线性融合仍不稳定，这增强了而非削弱了结论；其二，仿射不变（= 0）是解析结论而非经验发现，本文以其为对照而非贡献；其三，分位公制的零漂移同样是设计目标（秩不变性），本实验以有限样本复算**验证**之，而非发现之。综上，本节的角色是**方法验证**，真实系统上的检验由第 5 节承担。

---

## 5 真实数据检验一：公制修复与两条负结果

### 5.1 数据、许可与样本

| 数据集 | 规模 | 本文用途 | 许可与引用义务 |
|---|---|---|---|
| assist09（修正版） | 346,860 行 × 31 列（64,412,812 B） | 三源相关性、O1 公制漂移、滑窗错误率分布 | 须引 Feng 等（2009）与数据页（URL 投稿前核验） |
| DBE-KT22 | 161,953 条有效作答；212 题；1,264 名学生 | O2 难度估计器、O4 / O4b 多信号融合 | 须引 arXiv:2208.12651；ADA Dataverse DOI 10.26193/6DZWOH |
| XES3G5M | 4,806 题（≥100 次作答且有知识组件路径）；7,652 题带题干 | O3 知识树结构负结果 | 须引 NeurIPS 2023 Datasets & Benchmarks；MIT 许可 |
| Junyi Academy | 16,217,311 行；72,758 名用户；1,326 个练习 | O5 跨系统复现、O6 样本效率与稳健性、O7 / O8 留出验证 | CC-BY-NC-SA-4.0（**禁商用**）；须引 Chang 等（2015） |

四个数据集覆盖了三种不同的证据制度，这本身构成了本文的外部效度设计：DBE-KT22 有专家难度标注（1/2/3 三级）且有学生自陈反馈（难度反馈与信任度）；Junyi 有专家难度标注（easy/normal/hard）但**无自陈反馈通道**；XES3G5M 与题干与知识树但无专家难度；assist09 有提示使用与多技能标注但无专家难度。第 6 节将利用这一设计把"有反馈"与"无反馈"两种制度分开报告。

### 5.2 真实难度源两两弱负相关：一个合成实验不可能出现的形态

在 assist09 的 994 道（≥50 次作答）题目上，三个现实可得的难度源之间的 Spearman 相关为：

| 源对 | Spearman ρ |
|---|---|
| IRT 难度 ~ 中位首答时长 | **−0.250** |
| IRT 难度 ~ 平均尝试次数 | −0.134 |
| 首答时长 ~ 平均尝试次数 | **−0.302** |

三个源**两两负相关**：被 IRT 判定为难的题目，学生反而答得更快、尝试次数更少。这与第 4.1 节合成实验中"四源为同一潜能力的相关单调观测"的设定恰好相反。其解释并不神秘——难题往往被学生更快放弃（放弃得快则时长短、尝试少），而容易但繁琐的题目反而耗时长、尝试多。重要的是后果：**真实系统的难度量表不仅不可公度，方向还互相打架**。在合成实验中，min-max 融合的漂移来自离散度差异；在真实数据中，它还要叠加符号冲突，因此第 5.3 节观测到的真实漂移（0.5357）远大于合成实验（0.1276）并不意外。这一发现也解释了为什么"把某个源做得更像 logit"之类的局部改良会失效：当源之间负相关时，任何依赖具体数值的线性组合都会把噪声当成信号加权。

### 5.3 O1：从 0.5357 到 0.0001

在同一批 994 道题目上，比较两种公制在任意单调重参数化下的有效权重漂移：

| 融合公制 | L1 均值 | 排序反转 |
|---|---|---|
| A：min-max 线性（现状） | **0.5357** | 显著 |
| B：经验 CDF → logit（分位链接） | **0.0001** | **0** |

**这是命题 C1 的工程落地证据，且是在真实数据上取得的。** 把难度融合模块从 min-max 线性融合换成"分位链接 + logit 公制"之后，有效权重在任意单调重参数化下的漂移从 0.5357 降到 1 × 10⁻⁴ 量级（纯数值噪声），排序反转归零。与合成实验（0.1276 → 0）相比，真实数据的基线漂移高出约 4.2 倍，而修复后的残余漂移同为零——即**公制修复在真实数据上的收益比在合成环境中更大**。

此处必须重述文首的口径更正：早期一批实验因把不同难度源的符号约定混用（部分源正方向为"易"，部分为"难"），得到的线性融合漂移被系统性抬高；统一为"正 = 难"后重算为 0.5357。本文不引用被更正的旧数值。定性结论在两种口径下一致（线性融合的漂移远大于分位公制），但只有 0.5357 是口径一致、可作比较的数字。

### 5.4 O2：换估计器只值 +0.009

在 DBE-KT22（1,264 名学生 × 212 题）上，以专家难度标注（1 易 / 2 中 / 3 难）为判据比较两种难度估计器：

| 估计器 | 与专家等级的 Spearman ρ |
|---|---|
| 原始正确率难度（基线） | 0.2207 |
| **联合 IRT-1PL（EM 式梯度，300 轮）** | **0.2300** |

IRT 分层均值（正 = 难）为 level 1 → −1.32、level 2 → −1.19、level 3 → −0.21，**单调且 hard 层分离明显**；正确率按专家等级单调递减（0.803 / 0.770 / 0.632），与原数据集论文的结论吻合。

**诚实解读：ρ 只提升 0.009。** 这是一个负向信息量很大的结果：它说明在"专家标注 vs 行为数据"这条轴上，瓶颈不在估计器的复杂程度，而在专家标注本身与作答行为的一致度。这直接否定了本文原预注册中"换更好的测量模型可以改善可公度性"的隐含预期，并促成了第 6 节的转向——既然估计器不是瓶颈，就应当去检验**信号集**是否是瓶颈。后文将表明，这一转向既带来了第 6 节的增益，也带来了第 7 节的反转。

### 5.5 O3：知识树结构不是难度序

在 XES3G5M 上检验一个在工程实践中被广泛默认、却极少被显式检验的假设：知识组件（KC）树的层级结构能否充当难度代理。

- **深度相关性**：KC 树深度与经验难度的 Spearman ρ = **+0.0785**（p = 5.1 × 10⁻⁸，显著但极弱）；分层均值**非单调**——depth 4 的正确率为 0.799（最易），depth 3 为 0.746（最难）。
- **树边梯度**：在 347 条父子边上，"父节点难度 < 子节点难度"的比例为 **0.487**，约等于抛硬币。

两条证据共同证伪"树结构 = 难度序"。本文把它作为一条小型但干净的负结果报告：它不依赖任何新算法，只依赖一次相关性检验与一次方向一致性检验，却足以阻止一类常见的工程捷径——用知识树深度、章节编号或课程进度代替难度估计。这一负结果已随代码落地写入被测系统难度公制层的模块文档（第 9.2 节），作为禁止性约束。

---

## 6 真实数据检验二：多信号融合与专家标签判据下的增益

### 6.1 O4：DBE-KT22 上的五信号融合与一个差分特征

O2 表明换估计器收效甚微。O4 检验另一个假设：**瓶颈可能不在标签，而在单一行为信号（正确率）本身信息量不足**。设计如下：在 DBE-KT22 的 212 道题（每题 ≥30 条交易）上取五个行为信号——成功率（反向）、提示率、学生自报难度、信任度（反向）、平均答题时长——各自经 ECDF → logit 变换（即 O1 的分位公制，正方向 = 难）后线性融合。

| 估计器 | 与专家 1/2/3 的 Spearman ρ |
|---|---|
| 原始正确率（O2 基线） | 0.2207 |
| 联合 IRT-1PL（O2） | 0.2300 |
| **五信号等权融合** | **0.2899** |
| 优化权重（贪心，样本内） | 0.5306 |
| **优化权重 · 重复 5 折 ×10 交叉验证** | **0.5240 ± 0.1069**（区间 [0.2895, 0.7390]） |

单信号表现为：信任度（反向）0.261、时长 0.240、成功率（反向）0.217、提示率 **−0.117**（方向相反）、自报难度 0.038（单独近乎噪声）。

**核心发现是一个差分特征。** 自报难度与信任度在题级强正相关（Pearson +0.94），但在 ECDF 空间中"信任度反向"与自报难度的相关为 **−0.80**。优化权重选中的 50/50 组合本质上是一个差分特征：**"自报难度高 且 信任度低"的题目才是真难**。两个弱信号相减，消掉了共同模态的噪声，隔离出难度特异方差，使 ρ 从单个信号的上限 0.26 跃升至 0.53 量级（交叉验证 0.52）。

三点引用纪律必须遵守：样本内的 0.5306 有乐观偏置，**论文引用交叉验证值**；等权 0.2899 是免调参下界，代表"不调参也能拿到的部分"；自报难度与信任度来自同一系统的学生自评，与专家标注独立（不构成标签泄漏），但**仅存在于带反馈机制的系统**——无反馈系统只能退到纯性能信号融合（约 0.29）。

### 6.2 O4b：扩展特征不增益，差分特征是驱动者

为检验上节的机制解释而非仅接受其拟合结果，O4b 增加三个显式特征（显式差分项、离散度、作答量）并用坐标上升重估：

| 配置 | 重复口径下的交叉验证 ρ | 说明 |
|---|---|---|
| O4（5 信号全网格贪心） | **0.4778**（单次 5 折，可引用须注明口径） | 最终引用配置 |
| O4b（8 特征，坐标上升） | 0.4608 | 未超过 O4 |

两个直接证据支持"差分特征是驱动者"这一解释：显式差分特征（自报难度 − 信任度）单独 ρ = **0.389**，为最强单特征；作答量与专家难度负相关（ρ = −0.302），说明热门练习往往是核心简单题。新增特征不带来可泛化增益，故 O4 保持为最终引用配置，O4b 仅作为机制消融证据。

### 6.3 O5：Junyi 上的跨系统复现（无反馈制度）

Junyi 与 DBE 的关键差异是：Junyi **没有学生自报反馈字段**，属"仅性能信号"制度。这恰好检验了 O4 在无反馈系统上能保留多少。

| 估计器（Junyi，1,234 个练习） | 与专家 easy/normal/hard 的 Spearman ρ |
|---|---|
| 仅成功率 | 0.2610 |
| **七信号等权融合** | **0.3629**（相对基线 +39%） |
| 坐标上升优化 · 重复 5 折 ×10 | **0.4023 ± 0.0480**（区间 [0.3227, 0.4909]） |

单信号为：重复会话率 0.294、平均尝试次数 0.285、降级率 0.271、成功率（反向）0.261、提示率 0.217、时长 0.214、升级率 **−0.184**（反向）。

**结论：** 融合增益**不是 DBE 特有的**。在 1,621 万条交互、1,234 道题、完全不同的学科（台湾数学）与不同制度（无反馈）上，等权融合相对基线的提升幅度（+39%）与 DBE（0.2207 → 0.2899，+31%）同量级。这为"难度估计应采用多信号秩融合而非单一正确率"提供了跨系统证据，同时界定了无反馈系统可达到的上限（约 0.36–0.40）。

需要预先提醒读者：本节所有增益的判据都是**专家难度标签**。第 7 节将把判据换成"未见过样本的真实表现"，届时的结论会反转。本节的表格数字本身无误，但其解释力边界由第 7 节重新划定。

### 6.4 O6a：样本效率——增益来自互补信息而非去噪

若融合的收益只是"多信号平均掉了噪声"，那么收益应当随样本量增大而消失。O6a 以蓄水池抽样给出每题任意 k ∈ {10, …, 500} 条作答的随机子样本（每档 30 次重复）来检验这一点。

| k | Junyi 仅成功率 | Junyi 融合 | 配对胜率 | DBE 仅成功率 | DBE 融合 | 配对胜率 |
|---|---|---|---|---|---|---|
| 10 | 0.1750 | 0.2260 | 1.00 | 0.2024 | 0.2316 | 0.87 |
| 25 | 0.2116 | 0.2640 | 1.00 | 0.2113 | 0.2399 | 0.77 |
| 50 | 0.2314 | 0.2929 | 1.00 | 0.2224 | 0.2493 | 0.83 |
| 100 | 0.2416 | 0.3099 | 1.00 | 0.2208 | 0.2654 | 0.97 |
| 250 | 0.2496 | 0.3269 | 1.00 | 0.2213 | 0.2725 | 1.00 |
| 500 | 0.2536 | 0.3380 | 1.00 | 0.2209 | 0.2779 | 1.00 |
| 全量 | 0.2610 | 0.3629 | — | 0.2170 | 0.2899 | — |

融合的**相对**增益在各样本量档上稳定在 +29% ~ +33%，绝对增益甚至随样本量单调上升（Junyi 从 +0.0509 到 +0.0844）。因此融合提供的是**互补信息**，而非仅降低方差——这一判断在第 7 节会被限定但不会被推翻：互补性是真的，只是它补的是"专家标签所含的构念"，而不必然是"学生表现所含的构念"。

**修正记录（诚实存档）**：DBE 的首版样本效率结果虚高（k = 500 给出 0.2485，超过全量 0.2170）。原因是 212 题中有 13 题的作答数 n < K = 500，蓄水池尾部为补零槽，先全局置换再截断会把补零当作 0 计入求和。改为"无效槽排序键置 +∞ 排到队尾"后取数，上表为修正后数值。

### 6.5 O6b：统计稳健性（bootstrap 2000 次与重复交叉验证）

| 量 | Junyi | DBE |
|---|---|---|
| 仅成功率 ρ [95% CI] | 0.2606 [0.2112, 0.3101] | 0.2167 [0.0809, 0.3476] |
| 等权融合 ρ [95% CI] | 0.3623 [0.3149, 0.4094] | 0.2890 [0.1563, 0.4127] |
| 配对差 Δ [95% CI] | **+0.1017 [0.0726, 0.1325]** | **+0.0723 [−0.0195, 0.1649]** |
| 单侧 p(Δ ≤ 0) | **0.000** | **0.059** |
| 三档二次加权 Kappa（成功率 → 融合） | 0.1815 → 0.2595 | 0.1778 → 0.2370 |
| 重复 5 折 ×10（50 折）优化权重 ρ | **0.4023 ± 0.0480** | **0.5240 ± 0.1069** |

两点必须强调。其一，**DBE 上的融合增益在专家标签判据下也仅边缘显著**（单侧 p = 0.059，置信区间跨 0），Junyi 上的 p = 0.000 才构成强证据；把两个数据集的增益合并叙述而不区分显著性，是对证据强度的美化。其二，二次加权 Kappa 的绝对水平（0.18 → 0.26）远低于"可用"的直觉门槛，说明三档分类意义上的改善有限，改善主要发生在秩层面。

**数字更新（必须遵守）**：O5 报告的 0.3837 与 O4 报告的 0.4778 均为**单次 5 折**结果；重复 5 折 ×10 的更稳估计为 Junyi 0.4023 ± 0.0480 与 DBE 0.5240 ± 0.1069。两者与旧值相差均在 1 个标准差内（不矛盾），但**本文一律优先引用重复值并给出标准差**；旧值若出现，须注明"单次 5 折"。

---

## 7 判据切换：留出验证、结论反转与一处方向性错误

### 7.1 专家标签与真实表现难度之间的距离

O4 与 O5 的一致性判据是**专家难度标签**。但自适应排序真正需要的是"预测真实学生表现"。这两者不是同一构念，其距离可以直接测量：

> **专家难度标签 vs 真实表现难度（Junyi，能力十分位 5，n = 906，≥500 次作答）：Spearman ρ = 0.4326（95% CI [0.38, 0.48]，p < 0.001）。**

即专家标签只解释了真实表现难度约 19% 的秩方差。这不是对专家标注的批评——专家标注衡量的是"课程设计者认为的难"，它与"学生实际做起来有多难"本就是两个构念。但它意味着：**一个估计器在"贴合专家标签"上的进步，未必转化为"预测学生表现"的进步**。若不先把这条距离测出来就报告一致性增益，读者会默认两者是同一件事——本文在此明确否认这一默认。

### 7.2 O7：留出验证下的结论反转

做法是把每道题的蓄水池槽位切成**互斥**的 A 段（用于估计，k 条）与 B 段（用于判据，250 条），使估计误差与判据误差不再共享样本。判据换成"未见过样本的真实表现"后：

| 数据集 | k | 仅成功率 | 等权融合 | 差 | 融合胜率 |
|---|---|---|---|---|---|
| Junyi | 10 | 0.6677 | 0.6410 | −0.0267 | 0.00 |
| Junyi | 25 | 0.8066 | 0.7355 | −0.0711 | 0.00 |
| Junyi | 50 | 0.8814 | 0.7850 | −0.0963 | 0.00 |
| Junyi | 100 | 0.9251 | 0.8115 | −0.1136 | 0.00 |
| Junyi | 200 | 0.9509 | 0.8287 | **−0.1222** | 0.00 |
| DBE | 10 | 0.8083 | 0.6331 | −0.1752 | 0.00 |
| DBE | 25 | 0.9036 | 0.6982 | −0.2054 | 0.00 |
| DBE | 50 | 0.9427 | 0.7259 | −0.2168 | 0.00 |
| DBE | 100 | 0.9652 | 0.7541 | −0.2112 | 0.00 |
| DBE | 200 | 0.9767 | 0.7655 | **−0.2113** | 0.00 |

**结论：以"泛化到新样本"为判据，等权七信号融合全面劣于仅用成功率**，且样本越充足劣势越大（Junyi 从 −0.0267 扩大到 −0.1222，DBE 稳定在 −0.21 附近），10 个配置中融合胜率全部为 0.00。因此本文对 O4 / O5 的最终定性是：**那些增益是与专家标签的一致性增益，不是预测力增益**。这句话不是措辞上的保守，而是由 10 个配置的一致方向强制给出的。

一个必须同时避免的过度解读是："融合无用"。融合在专家标签判据上的增益是真实的、可复现的、且具有明确机制（差分特征）；它无用，是指它在**自适应排序**这一用途上未被证明有用。对于"题目入库时的难度分级""题库质量的专家对齐"这类以专家判断为目标的任务，融合仍然有效。

### 7.3 O8：一处方向性错误的定位与修复

面对上述反转，本文没有把它归为"调参偏好"，而是去检查融合配置本身。检查定位到一处实打实的错误：

> **升级率与专家难度的相关为 −0.1837，在池内（同知识点 × 同能力十分位）为 −0.756，却被优化器赋予了 +0.154 的正权重。**

即一个方向为负的信号被以正的权重计入了融合。这在样本内拟合中不会暴露（优化器可以靠其他信号补偿），但在留出验证中会系统性地把难度的方向搞反。修复方式有二：剔除该信号（fused6），并引入随样本量收缩的融合权重 λ（第 7.4 节）。在留出框架下比较变体（λ 在随机一半题目上选、留出另一半上评，20 次折半 × 25 次重复）：

| 变体 | k | 成功率 ρ / 变体 ρ | λ\* | 留出 ρ(λ\*) | 相对成功率增益 |
|---|---|---|---|---|---|
| fused7（O5 原配置） | 10 | 0.6702 / 0.6431 | 0.52 | 0.6871 | +1.73 pp |
| **fused6（剔除升级率）** | 10 | 0.6659 / **0.6880** | 0.70 | **0.6992** | **+3.43 pp** |
| fused6 | 25 | 0.8109 / 0.7974 | 0.52 | 0.8273 | +1.57 pp |
| fused6 | 50 | 0.8810 / 0.8487 | 0.39 | 0.8878 | +0.73 pp |
| fused6 | 100 | 0.9257 / 0.8748 | 0.23 | 0.9271 | +0.22 pp |
| fused6 | 200 | 0.9504 / 0.8899 | 0.12 | 0.9502 | +0.05 pp |
| fused3（成功率 + 提示 + 尝试） | 10–200 | — | 0.04–0.08 | — | ≈ 0（无增益） |

三点结论。第一，**剔除反向信号后，融合在 k = 10 时首次反超成功率**（0.6880 对 0.6659，+0.0220），各 k 的落后幅度也减半（k = 200 时由 −0.1206 收窄到 −0.0605）。第二，**不是"信号越少越好"**——fused3 反而无增益，因为变体与成功率越相似，可获得的互补增益越小；真正的修复点是**方向性校验**，不是缩减信号数。第三，收益高度依赖样本量，这引出下一节。

### 7.4 样本量自适应收缩 λ\*(k)

λ\* 从 k = 10 的 0.70 单调降到 k = 200 的 0.12，说明"融合该占多大比重"是样本量的函数。拟合式为

  **λ\*(k) = 1 / (1 + (k / 27.4)^0.902)，R² = 0.9989**，

观测值 0.701 / 0.524 / 0.387 / 0.234 / 0.125 对应拟合值 0.713 / 0.521 / 0.368 / 0.237 / 0.143。该拟合是否可以直接部署（而不是只在参与拟合的那批 k 上成立），由第 7.5 节的留一 k 交叉验证（区间内插）检验回答。

本文据此给出的推荐估计器为：

```
score(item) = (1 − λ) · z_success(item) + λ · Σ_{s ∈ 7 信号} w_s · sgn_s · z_s(item)
sgn_s = sign(Spearman(z_s, z_success))      # 方向校正；success 自身 sgn = +1
w_s   = max(0, 折半信度_s) 归一             # 折半信度在拟合块 A 内估计
λ = 1 / (1 + (k / 27.3)^0.895)              # O9 留一 k 交叉验证拟合值；全量拟合 27.4 / 0.902
信号集 = 全部 7 个信号（含升级率，被符号校正为负贡献）
z_s = ECDF → logit（O1 的单调重参数化不变公制）
```

式中 λ 的参数取**留一 k 交叉验证拟合值**（k₀ = 27.3、p = 0.895），而不是上面的全量拟合值（27.4 / 0.902）；两者几乎一致，说明曲线形状稳定，但全量拟合值是在同一批 k 上既拟合又评估得来的，故部署时优先取交叉验证值。该闭式同样是对 O8 的 fused6 逐 k 经验 λ\*(k) 的拟合（λ 的取法与融合内核相互独立，O11 确认闭式 λ_cf 对 `srw7` 内核同样适用，见第 7.8 节），留一 k 交叉验证确认的是经验 λ\* 的区间内插泛化，而非闭式本身的直接验证——该直接验证现已补做：将 λ_cf(k)=1/(1+(k/27.3)^0.895) 固定代入同一套留出协议（不在数据上拟合 λ、在留出半上评估），其在 k=10/25/50/100/200 的留出 Spearman 与经验最优 λ 版本差距分别为 +0.34/−0.55/+0.12/+0.01/+0.07 pp（均 ≤0.55pp），确认闭式即可部署，无需逐 k 拟合 λ。下节给出这一选择的经验依据。上述估计器中的**全部符号、权重与 λ 都只在拟合块 A 内估计**，判据块 B 完全未参与，因此不存在泄漏；这一点由第 7.8 节的留出协议与 O11 的逐项复现共同保证。

### 7.5 λ\*(k) 的留一 k 交叉验证（O9）：区间内插检验，公式可直接部署

第 7.4 节的 λ\*(k) 是在同一批 k 上既拟合又评估得到的，不能直接当作部署规则。O9 的第一项检验正是为此：只用 k ∈ {10, 50, 200} 的 λ\* 拟合出 (k₀, p)，再在拟合区间 [10, 200] 内**留有验证**地评估到未参与参数估计的 k = 25 与 k = 100（属区间内插，而非区间外推——真正的样本外检验由折半留出完成，见 7.6 节）。

| 验证 k（区间内插） | λ̂（公式） | λ\*_emp（折半挑出） | ρ(λ=0) | ρ(λ̂) | 公式收益 | 经验收益 | 保留比例 |
|---|---|---|---|---|---|---|---|
| 25 | 0.520 | 0.536 | 0.8103 | **0.8272** | **+1.695 pp** | +1.528 pp | **111%** |
| 100 | 0.239 | 0.230 | 0.9262 | **0.9286** | **+0.237 pp** | +0.186 pp | **128%** |

拟合参数（留一 k 交叉验证）为 k₀ = 27.332、p = 0.895，与全量拟合的 27.4 / 0.902 几乎一致，说明曲线形状稳定。**公式区内插的 λ̂ 反而略优于折半挑出的经验 λ\***（保留比例 111% 与 128%），原因是经验 λ\* 自身带选择噪声。**结论：λ\*(k) 无需按部署重新调参即可直接使用。**

### 7.6 学生级留出验证（O10）：记录级担忧的经验检验

第 7.2 节的留出验证切的是**记录**而不是**学生**：同一学生的多次作答可能同时落在 A、B 两段，两组误差并不独立，因而"泛化估计偏乐观"是一个合理的攻击点。O10 正面处理这一缺口：按 **crc32(uuid) 奇偶**把 72,758 名学生切成互斥两半，估计只用 A 半学生在该练习上的前 k 条作答（半内蓄水池 250），判据只用 B 半学生的**全部**作答计算真实难度 1 − ok_B/n_B（≥500 条）；纳入 **1,238 个练习**（判据半区要求 ≥500 条作答，与第 6.3 节的 1,234 口径不同），25 次重复 × 20 次折半。

> **实现注记（可复现性的一部分）**：学生分半**不能**使用 Python 内置 `hash()`——它对字符串按进程随机加盐（PYTHONHASHSEED），切分不可复现；本脚本改用 crc32 并按 uuid 缓存。若忽略这一点，同一份代码在不同进程里会得到不同的分半，结论将不可复现。

| k | 仅成功率 | fused7（O5 配置） | fused6 | mix6（λ̂ 公式，**未调参**） | λ̂ | λ\*_emp | λ̂ 增益 | fused6 原始增益 |
|---|---|---|---|---|---|---|---|---|
| 10 | 0.6631 | 0.6442 | 0.6870 | **0.6995** | 0.711 | 0.716 | **+3.64 pp** | +2.39 pp |
| 25 | 0.8133 | 0.7418 | 0.7993 | **0.8297** | 0.520 | 0.521 | +1.64 pp | −1.40 pp |
| 50 | 0.8881 | 0.7838 | 0.8491 | 0.8944 | 0.368 | 0.355 | +0.63 pp | −3.90 pp |
| 100 | 0.9356 | 0.8101 | 0.8783 | 0.9372 | 0.239 | 0.200 | +0.16 pp | −5.73 pp |
| 200 | 0.9603 | 0.8234 | 0.8922 | 0.9605 | 0.144 | 0.093 | +0.02 pp | −6.81 pp |

三点结论。**第一，O7 的结论在学生级留出上完整复现**：fused7 依然全线落后，且样本越充足劣势越大（k = 200 时 0.8234 对 0.9603，差 **−0.137**）；λ 收缩的收益与记录级几乎一致（k = 10 时 **+3.64 pp**，记录级为 +3.43 pp）。因此"记录级而非学生级"这一担忧经检验**影响很小**，不构成对本文结论的实质威胁。**第二，λ̂ 与经验 λ\* 高度吻合**（0.711 / 0.716、0.520 / 0.521、0.368 / 0.355），在**完全不调参**的前提下拿到了几乎全部收益，进一步支持 λ\*(k) 公式可直接部署。**第三，"只剔除方向错误仍不够"这一点也被复现**：fused6 在 k ≥ 25 后仍为负增益（−1.40 至 −6.81 pp），只有叠加 λ 收缩后的 mix6 才在全部 k 上不劣于仅用成功率——即方向性校验与样本量收缩是两件缺一不可的事。第 7.8 节进一步表明存在更优变体 `srw7`：它同样在全部 k 上不劣于基线，且在所有 k 上高于 mix6 所用的 fused6 内核（学生级 k = 10/25/50/100/200 分别为 +4.283 / +2.189 / +0.951 / +0.335 / +0.102 pp），说明"剔除"并非方向性错误的最优处置。

### 7.7 三条边界（必须与结论同时报告）

**其一，增益随样本量迅速消失。** 按本文最终推荐的 `srw7` 估计器计，留出增益在 k = 100 时为学生级 +0.335 pp、记录级 +0.391 pp，k = 200 时降至学生级 +0.102 pp、记录级 +0.138 pp（第 7.8 节表 7-1、表 7-2）。对成熟题库（每题数百条观测）**不应**预期显著收益——此时成功率已接近充分统计量，互补空间很小。

**其二，DBE 上 λ 收缩无收益。** DBE 的 λ\* 仅在 0.05–0.17，留出 ρ 与 λ = 0 持平甚至略低（k = 25 时 0.9001 对 0.9006）。该改进**只在 Junyi 上得到验证，未跨系统复现**。本文不把它写成通用规则。（与之呼应，DBE 重复交叉验证的置信区间 [0.2895, 0.7390] 跨度约 0.45、n = 212 题下信息量极低，进一步说明 DBE 上的估计不可与 Junyi 合并叙述。）

**其三，"记录级留出"的效度缺口已由学生级留出检验处理，结论保持。** 第 7.6 节的 O10 按学生切分重做了全部检验，O7 的结论完整复现，λ 收缩收益与记录级几乎一致（k = 10 时 +3.64 pp 对 +3.43 pp），故该担忧经检验**影响很小**。剩余边界有三条：**一是**收益衰减——学生级口径下 `srw7` 的增益在 k = 100 时为 +0.335 个百分点、k = 200 时为 +0.102 个百分点（记录级为 +0.391 / +0.138 个百分点）；**二是**跨系统外推——λ 收缩只在 Junyi 上得到验证，DBE 上无收益；**三是** O10 仍以学生–题目配对在历史数据内重排，未覆盖"新学生 + 新题"的冷启动交叉泛化——该场景下融合是否仍有收益仍是开放问题。此外，判据段固定 250 条（O7）与判据半区固定作答量（O10）意味着判据自身有信度上限，因此两处的 ρ 绝对值（如 0.9603）不可解读为"接近完美"，只有**同 k 内的组间比较**有效。

### 7.8 O11/O12：融合估计器的优化——符号校正与折半信度加权

第 7.3 节把反转归因于一处方向性错误，并以"剔除升级率"（fused6）修复。O11 与 O12 检验一个更进一步的假设：**方向错误的信号应当被校正方向，而不是被整列删除**；在此之上，**按每个信号在拟合段内的可靠性加权**应能进一步降低小样本下的融合方差。两者都在第 7.2 节（O7/O8）的同一留出协议下评估：拟合块 A 为每题 k 个蓄水池槽位，判据块 B 为互斥的 250 条槽位；REP = 25、SPLITS = 20；O12 沿用 O10 协议（按 `crc32(uuid)` 奇偶二分学生，1,238 题，蓄水池/半 = 250，REP = 25 × SPLITS = 20）。**全部符号、权重与 λ 都只在拟合块 A 内估计，判据块 B 完全未参与，不存在泄漏**；λ 缺省取闭式 λ_cf(k) = 1 / (1 + (k / 27.3)^0.895)。

变体的定义如下：`fused6` 为 O8 的"剔除升级率的 6 信号等权"；`fused7` 为 O5 的 7 信号等权；`signed7` 为 7 信号等权但按拟合段内秩相关**校正每信号符号**（success 自身符号固定为 +1）；`rw6` 为 6 信号按折半信度加权；`srw7` 为 7 信号同时做符号校正与折半信度加权，即

```
score(item) = (1 − λ) · z_success(item) + λ · Σ_{s ∈ 7 信号} w_s · sgn_s · z_s(item)
sgn_s = sign(Spearman(z_s, z_success))      # 方向校正；success 自身 sgn = +1
w_s   = max(0, 折半信度_s) 归一             # 折半信度在拟合块 A 内估计
λ = 1 / (1 + (k / 27.3)^0.895)              # O9 留一 k 交叉验证拟合值；全量拟合 27.4 / 0.902
```

表 7-1 给出记录级留出增益，表 7-2 给出学生级留出增益。

**表 7-1 记录级留出增益（O11，相对"仅成功率"基线，单位 pp，λ = λ_cf）**

| k | 仅成功率(held-out) | fused6 | fused7 | signed7 | rw6 | **srw7** | λ_cf | λ_rel | λ\*_emp |
|---|---|---|---|---|---|---|---|---|---|
| 10 | 0.6698 | +3.278 | +1.498 | +3.594 | +3.616 | **+3.823** | 0.7107 | 0.7010 | 0.7000 |
| 25 | 0.8060 | +1.586 | +0.523 | +1.930 | +1.829 | **+2.025** | 0.5197 | 0.4902 | 0.5270 |
| 50 | 0.8824 | +0.659 | +0.181 | +0.850 | +0.806 | **+0.930** | 0.3678 | 0.3136 | 0.3723 |
| 100 | 0.9248 | +0.243 | +0.073 | +0.327 | +0.335 | **+0.391** | 0.2383 | 0.1881 | 0.2370 |
| 200 | 0.9502 | +0.071 | +0.013 | +0.111 | +0.108 | **+0.138** | 0.1440 | 0.1041 | 0.1333 |

> 复算命令：`python results/code/o11_fusion_optimization.py`（输出：`results/code/o11_fusion_optimization.json`）。该脚本依赖 numpy，须用装有 numpy 的解释器运行（项目 venv 未装 numpy；本轮实测由 Python 3.13 + numpy 2.5.3 运行通过）。

**表 7-2 学生级留出增益（O12，单位 pp）**

| k | 仅成功率 | fused6 | **srw7（λ_cf）** | srw7 − fused6 | 胜率 | Wilcoxon p |
|---|---|---|---|---|---|---|
| 10 | 0.6631 | +2.390 | **+4.283** | +0.662 | 0.902 | 1.15e-66 |
| 25 | 0.8133 | −1.402 | **+2.189** | +0.557 | 1.000 | 1.26e-83 |
| 50 | 0.8881 | −3.905 | **+0.951** | +0.317 | 1.000 | 1.26e-83 |
| 100 | 0.9356 | −5.728 | **+0.335** | +0.169 | 0.998 | 1.27e-83 |
| 200 | 0.9603 | −6.804 | **+0.102** | +0.081 | 0.996 | 1.29e-83 |

> 复算命令：`python results/code/o12_student_o11.py`（输出：`results/code/o12_student_o11.json`）。该脚本依赖 numpy，须用装有 numpy 的解释器运行（项目 venv 未装 numpy）。
> 口径注记：第 2 列 `fused6` 为**未叠加 λ** 的留出 ρ 与基线之差；第 3 列 `srw7（λ_cf）` 为**已叠加 λ_cf** 的留出 ρ 与基线之差；而 `srw7 − fused6`、胜率与 Wilcoxon p 为**配对口径**——在同一 λ_cf 下，对每一折半 B 分别计算 `srw7` 与 `fused6` 的留出 ρ 再取差，跨 25 × 20 = 500 个配对取均值与符号秩检验。因此 `srw7 − fused6` 列**不能**由第 2、3 列相减得到（相减得 1.893 pp，配对值为 0.662 pp，见 k = 10），这是两处口径不同所致，不是数值矛盾。

**结论一：符号校正优于直接剔除，且在全部 k 上成立。** `signed7 > fused6` 在 k = 10/25/50/100/200 上逐档成立（k = 10：+3.594 对 +3.278 pp；k = 200：+0.111 对 +0.071 pp）。这说明 O8 的"剔除升级率"虽然纠正了方向，却把该信号的**信息一并丢弃**了——它的错误在方向而不在信息量；把方向校正过来、作为负向证据计入融合，反而优于整列删除。

**结论二：可靠性加权再加成，`srw7` 在所有 k 上最优。** 在符号校正之上按折半信度加权（权重 = 每信号在拟合块 A 内对半切分后两个半段估计之间的秩相关，取正后归一）后，`srw7` 在全部 k 上取得最高增益：+3.823 / +2.025 / +0.930 / +0.391 / +0.138 pp（记录级，表 7-1）。低信度信号自动降权，从而压低小样本下的融合方差。

**结论三：λ 策略几乎等价，闭式即可部署。** 闭式 λ_cf 与经验最优 λ\*_emp 在所有 k 上的取值差距 ≤ 0.011（k = 10：0.7107 对 0.7000）；可靠性驱动 λ_rel = 1 − ρ_success(A) 与二者最大相差 **0.059**（k = 50：0.3136 对 0.3723）。尽管 λ 取值本身有这一量级的差异，三者的**留出增益**在所有 k 上相差 ≤ 0.087 pp（`srw7` 口径，最大出现在 k = 10），故融合权重的选择对 λ 的取法不敏感，据样本量直接代入闭式即可，无需逐 k 调参。

**结论四：O12 学生级复用同一优化，`srw7` 消除了 O8 估计器的有害性。** 在学生级留出下，`fused6` 在 k ≥ 25 时**低于"仅成功率"基线**（−1.402 / −3.905 / −5.728 / −6.804 pp），即该简化估计器随样本量增大反而有害；`srw7` 是同协议下**唯一在所有 k 上均不劣于基线**的变体（+4.283 / +2.189 / +0.951 / +0.335 / +0.102 pp）。`srw7` 相对 `fused6` 的配对胜率为 0.902 / 1.000 / 1.000 / 0.998 / 0.996，Wilcoxon 符号秩检验 p ≤ 1.29 × 10⁻⁶⁶。

**必须单列的局限。** 其一，O11/O12 **只在 Junyi 上运行**，未跨系统复现（DBE 上无对应验证）；其二，k = 200 时增益已经很小（记录级 +0.138 pp、学生级 +0.102 pp），对成熟题库不应期待显著收益；其三，判据段自身的信度上限（O7 判据段固定 250 条、O12 判据半区固定作答量）限制了 ρ 的绝对水平，因此表 7-1 与表 7-2 中**只有同 k 内的组间比较**有效，ρ 的绝对值不可解读为"接近完美"。

上述 `srw7` 估计器已作为增量函数落地到被测系统的难度公制层，落地方式、代价与校验见第 9.2 节。

---

## 8 最优错误率：描述性证据与未竟的因果识别

### 8.1 assist09 上的滑窗错误率分布

在 assist09（按学生、按 order_id 排序，取 ≥20 题者）上以 W = 20 作滑动窗口，计算每窗的错误率：

| 统计量 | 值 |
|---|---|
| 平均错误率 | **0.3511** |
| 中位错误率 | 0.3000 |
| **众数区间** | **0.15–0.20（成功率 80–85%）** |
| 成功率落在 77–83% 的窗口占比 | 10.5% |
| 成功率落在 80–90% 的窗口占比 | 15.4% |

**众数区间在三种过滤口径下稳健**：全量 mean_err 0.351、仅主题 0.333、无求助题 0.218，三种口径下众数区间**都是 0.15–0.20**。这说明众数位置不是由某一类题目（如求助题）驱动的。

### 8.2 这个分布说明了什么，不说明什么

它说明的两件事是保守的。第一，**大量学习窗口处于"过度困难"区间**——平均错误率 0.3511 远高于任何版本的最优值，中位 0.30 亦然；只有 15.4% 的窗口落在成功率 80–90%，10.5% 落在 77–83%。换言之，真实系统分配出去的难度，其分布中心并不在理论最优附近，这一点与"系统普遍偏难"的直觉一致。第二，**分布的众数恰好落在 80–85% 成功率带**，这个带同时包含 Wilson 等的 84.13% 与 Baillifard 等的 80.7%，因此本分布与两者都相容。

它不说明的事情更重要：**众数区间的宽度（5 个百分点）大于 84.13% 与 80.7% 之间的差距（约 3.4 个百分点）**，因此本证据**无法在这两个候选最优值之间作判别**。任何"本文支持 85% 规则"或"本文支持 80.7%"的说法都超出了这个分布所能承载的信息量。此外，滑窗错误率是系统分配与学生作答的联合产物，不是对剂量的干预，因此它连"描述性剂量—反应"都不是，只是"描述性暴露分布"。

### 8.3 明确未执行的部分

原预注册的因果侧设计（H4–H7）**全部未执行**，包括：以**前向标定**生成的事前错误率为剂量（θ 仅用 t 之前的作答滚动更新、β 以 item-split 交叉拟合）、以掌握速度为结局、以 AIPW/DR 估计剂量—反应曲线、以自助法构造 ê\* 的置信区间、以二次型与样条检验非单调性、以及五前提（二分类 / 无提示 / 无跳过 / 无遗忘 / 无迁移）的逐项分层分解。配套的稳健性程序（权重截断、有效样本量、阴性对照结局、E-value）亦未执行。

因此本文**不报告任何 ê\* 的点估计、置信区间或与 0.1587 的检验**，也不对"最优错误率在真实学习者上是否等于 15.87%"作任何方向的断言。本文在这一点上与原预注册的差异不是措辞上的谨慎，而是实质性的：预注册承诺的估计量没有产出，本文就不写它的结果。

### 8.4 预注册假设的落实情况对照

为便于读者判断本文与原设计的关系，下表逐条给出七个假设的落实状态：

| 假设 | 内容 | 状态 |
|---|---|---|
| H1 | 线性融合在单调重参数化下不稳定 | **已检验并成立**（第 4 节合成、第 5.3 节真实） |
| H2 | 统一量纲改善校准质量与目标命中率 | **部分检验**：ECE / Brier / 可靠性图 / 85% 目标命中率未计算；改为以专家标签一致度比较融合方案（第 6 节），判据已更换 |
| H3 | 四源校准误差存在稳定排序 | **未执行**：真实数据的源集与预注册设想的四源不同，ECE 未计算 |
| H4 | 最优错误率显著高于 15.87% | **未执行**（第 8.3 节） |
| H5 | 剂量—反应曲线非单调 | **未执行** |
| H6 | 五前提的效应衰减排序 | **未执行** |
| H7 | 能力分层下的最优区间异质性 | **未执行** |
| — | 附加发现：知识树结构不是难度序 | **已完成**（第 5.5 节，负结果） |
| — | 附加发现：融合增益的判据依赖性 | **已完成**（第 7 节，负结果） |

---

## 9 讨论

### 9.1 三项结论

**第一，可公度性是一个可以被修复的工程性质，而不是一个哲学立场。** 合成实验（L1 0.1276 → 0，36.4% 反转 → 0%）与真实数据（0.5357 → 0.0001）在两条独立路径上给出了同一结论：把各源从"归一化后线性相加"改为"分位链接到 logit 公制后相加"，有效权重就对任意单调重参数化不再漂移。这一改动不需要新的估计器、不需要新的数据、不需要调参，其代价只是放弃 min-max 这一习惯做法。本文认为它应当被视为难度融合的默认基线。

**第二，任何"难度估计改进了多少"的报告，必须同时报告改进的判据。** 本文在同一批数据、同一套融合配置上，先后得到了 +0.1017（专家标签判据，p = 0.000）与 −0.1222（留出表现判据，胜率 0.00）两个方向相反的结论。它们不矛盾，因为专家标签与真实表现难度的秩相关只有 0.4326。这一教训超出了难度估计本身：凡是以"与某个人类标注的一致度"为判据的研究，都应当先测出该标注与部署目标构念之间的距离，否则报告的增益可能只是对标注者视角的拟合。

**第三，测量侧的完成并不自动换来因果侧的成立。** 本文原设计的逻辑是"先论证度量是否成立，再论证该度量上的因果效应"。前一半完成了，后一半没有。这个不对称不应被叙述技巧掩盖：本文现在能说的是"难度量可以被良定义地融合"，不能说的是"把学生置于某个难度档会如何改变其掌握速度"。

### 9.2 对系统的含义与已落地的三处代码改动

上述结论中的一部分已落到被测系统的代码层，共三处（前两处由回归测试覆盖，第三处由外部校验脚本覆盖）：

1. **新增难度公制层** `learnflow-backend/app/services/difficulty_fusion.py`，提供三个函数：`ecdf_logit()`（经验 CDF → logit 的分位链接，对任意单调重参数化不变，对应 O1 的 1 × 10⁻⁴ 性质）、`fuse_signals()`（多行为信号秩融合，对应 O4 / O5 的结论）、`logit_to_fsrs()`（批内映射到 FSRS 的 [1, 10] 区间）。模块文档字符串已写入 O1 / O3 / O4 / O5 的数字，并写入 **O3 的负结果警告：禁止用知识树深度充当难度代理**。
2. **修改** `learnflow-backend/app/services/optimal_difficulty.py` 的 `update_task_difficulty`：把耗时的 `>180s` 硬阈值分档改为 log-time 上的连续单调软项 s = σ(ln(t/60)/0.8)，并加入提示惩罚（O5 显示提示率是有效难度信号）；旧的"快速答对则降低难度"行为契约保留。
3. **新增** `learnflow-backend/app/services/difficulty_fusion.py` 的 `estimate_optimized_difficulty()`（连同 `split_half_reliability()` 与 `OPT_SIGNAL_NAMES`，定义于 `:278`；该文件共 338 行）：实现第 7.8 节的 `srw7` 估计器——逐信号符号校正（按拟合块内与成功率方向的秩相关定号，反向信号取负而非剔除）、折半信度加权（`split_half_reliability()` 估计每信号信度，取正后归一）与 λ 收缩（缺省取闭式 `lambda_closed_form(k)`）。第 7.4 节的推荐估计器即此函数。

配套新增 `learnflow-backend/tests/test_difficulty_fusion.py`（14 条回归测试），覆盖分位公制的单调变换不变性、融合权重零漂移、融合优于单信号、耗时无跳变（179 s 与 181 s 的差 < 0.02）、证据分边界 [1, 4]；**上述回归测试只覆盖前两处改动**。全量测试**由改动前的 920 条增至 934 条**，实测 `pytest` 为 **934 passed**。本文所有引用一律使用 934。

**第三处落地的落地方式与代价（据实声明）**：`estimate_optimized_difficulty()` 以**增量函数**形式落地，**未新增 pytest 用例**，因此全量测试数**仍为 934**——早先"该估计器落地会改变测试数量"的预告不再成立，此处据实更正。该函数的正确性改由**外部校验脚本** `results/code/o11_land_verify.py` 保证：15 项检查全部通过（15/15 PASS），覆盖模块导出 `estimate_optimized_difficulty` / `split_half_reliability`、k ≤ 0 抛 `ValueError`、缺信号抛 `ValueError`、λ 缺省等于 `lambda_closed_form(k)`、`signed7` 与 `srw7` 在 k = 10/25/50/100/200 的留出增益与 O11 结果逐项复现（如 k = 10 时 +3.594 / +3.823 pp、k = 200 时 +0.111 / +0.138 pp），以及符号校正行为（校正后融合与难度方向正相关，Spearman +0.951）。

**一处被校验器发现并已修复的缺陷（据实记录）**：`ecdf_logit()` 原先以 `if not values:` 判空，对 numpy 数组输入会抛 `ValueError: The truth value of an array with more than one element is ambiguous`；现改为先取 `n = len(values)` 再判 `if n == 0: return []`，对 list 输入行为完全等价，不改变任何已测行为（`app/services/difficulty_fusion.py:63`）。

### 9.3 局限性

**第一，因果侧整体缺席。** 第 8.3 节已列明：剂量构造、结局定义、AIPW/DR 估计、非单调性检验、五前提分解与全部稳健性程序均未执行，本文因此不含任何关于最优错误率的因果结论。

**第二，融合增益的判据局限。** 第 7 节的所有结论都以"专家难度标签"与"留出表现"这两个特定判据为条件。其中"O7 切的是记录而非学生"这一效度缺口已由 O10 的学生级留出检验（第 7.6 节）处理——结论完整复现，故不构成实质威胁。剩余边界是判据自身的信度上限（O7 判据段固定 250 条，O10 判据半区固定作答量），它限制了所有 ρ 的绝对水平，只有**同 k 内的组间比较**有效。

**第三，λ 收缩与融合优化均未在跨系统上复现。** λ 收缩只在 Junyi（有降级率、重复会话率等信号）上得到验证，DBE 上无收益；第 7.8 节的 O11/O12 优化（符号校正 + 折半信度加权）同样**只在 Junyi 上验证过**，未跨系统复现。把它写进其他系统前必须重新验证。

**第四，专家标注本身的可疑地位。** 本文把专家标签当作可接受的判据使用，但第 7.1 节测出它与真实表现难度的相关仅 0.4326。这意味着以专家标签为准的一切比较（包括 O2、O4、O5、O6）都继承了这个局限。本文不认为存在更好的替代判据，但也不认为专家标签是"真值"。

**第五，数据许可约束。** Junyi 为 CC-BY-NC-SA-4.0，**禁止商用**；本文的全部分析为离线、非商用、只读研究用途。投稿前须逐条核验四个数据集的引用义务（assist09 的 Feng 等 2009 与数据页、DBE-KT22 的 arXiv:2208.12651 与 ADA DOI、XES3G5M 的 NeurIPS 2023 与 MIT 许可、Junyi 的 Chang 等 2015 与 CC 协议）。

**第六，被测系统的真实部署数据缺失。** 被测系统虽已建成并公开归档，但没有真实用户产生的日志，因此本文的全部实测都建立在四个公开数据集上；被测系统在本文中的角色是"被批评的一类实现"的具体实例与代码落地的载体，而非实证对象。

**第七，学习成瘾倾向测量未验证（强制披露）。** 研究依托系统内置的学习成瘾指数（LAI）为本项目自行构建，五维权重为时间 30% / 动机 25% / 控制 25% / 认知 10% / 功能 10%，其中"控制 + 功能"合计 35% 由自陈测量层供给（量表目录 `instrument_catalog.py` 中的 SRL-STOP / SLEEP-IMPACT / SOCIAL-IMPACT / TIME-BIAS）；未被测量的维度不计入加权并输出 coverage 予以披露，绝不以"健康"静默补分。**必须强调**：该量表的题项为本项目自编，尚未完成信度与效度验证，无常模、无临床截断值，属开发中的测量工具（work-in-progress instrument）。**本文未使用 LAI 作任何分析**——本文的全部数据来自四个公开数据集，不含自陈量表数据；此处披露仅为说明该工具的状态，防止读者误以为系统已具备经过验证的问题性使用测量能力。缓解路径为三层并进，且**均未完成**：①以公开已验证量表作聚合效度锚点（BStAS 5 题，Atroszko 等，2025；SI-10，Loscalzo 等，2024；中文青少年学习倦怠量表，吴艳等，2010）；②完成最小信效度集（α/ω + EFA→CFA + 2 周重测 ICC + 与倦怠/焦虑的相关）；③以自陈分数与行为日志做收敛效度。

### 9.4 一条实践建议

综合第 5.3 节、第 7.3 节、第 7.7 节与第 7.8 节，本文对难度融合模块的实践建议只有一句：**先把公制换成分位 logit（无条件），再按样本量决定是否启用多信号融合（有条件），并在启用前对每个信号做方向性校正而非简单剔除（强制）**。其中第一条的收益最大且不依赖任何调参；第二条的收益在每题观测数 ≥100 时趋近于零；第三条是一处**方向性配置错误**——第 7.8 节表明，把方向错误的信号整列剔除（O8 的 fused6）会丢掉其有效信息，把它的方向校正过来（作为负向证据计入）反而更优，即**校正优于剔除**。

---

## 10 诚实缺口清单

1. **因果侧未执行**：剂量—反应识别、AIPW/DR 估计、非单调性检验、五前提分解、E-value 与阴性对照全部未执行（第 8.3 节）。
2. **H2 / H3 未按原口径执行**：ECE、Brier、可靠性图与 85% 目标命中率未计算；四源校准误差排序未检验（第 8.4 节）。
3. **记录级留出的效度缺口已由 O10 的学生级留出检验处理**（第 7.6 节）：结论完整复现，"记录级而非学生级"的担忧经检验影响很小。剩余边界为 k ≥ 100 时增益衰减至 ≤0.34 pp（学生级 srw7 口径：k = 100 为 +0.335 pp、k = 200 为 +0.102 pp；记录级 +0.391 / +0.138 pp）与 λ 收缩未在 DBE 上复现（第 7.7 节）。
4. **λ 收缩未在 DBE 上复现**（第 7.5 节）。
5. **O11/O12 的优化未跨系统复现**：推荐估计器 `estimate_optimized_difficulty`（符号校正 + 折半信度加权 + λ 收缩）已**增量落地**到仓库代码，全量测试仍为 **934**，其正确性由外部校验脚本 `results/code/o11_land_verify.py`（15/15 PASS）保证（第 9.2 节）；但该优化只在 Junyi 上验证，未跨系统复现（第 9.3 节）。
6. **专家标签判据的信度未独立估计**：本文未获得专家标注的评分者信度，0.4326 这一距离的全部不确定性归于专家标签的单次实现。
7. **assist09 的滑窗分析使用"one row per student-problem"修正版**；求助记错（tutor 语义）按官方口径处理，与部分二手文献的口径可能不同。
8. **合成实验的最有利环境**：四源共享单一潜变量是命题成立的最有利设定（第 4.6 节）。
9. **数据许可未逐条法务核验**：四个数据集的引用义务与商用限制以官方页面为准，投稿前须闭环。
10. **LAI 未验证且本文未使用**（第 9.3 节第七条），三层缓解路径均未完成。

---

## 11 结论

本文把"难度能不能被放在同一把尺子上量"这一被工程实践跳过的问题，改写为一个可证伪的测量学命题，并在合成数据与四个真实学习系统上给出了检验结果。不稳定性命题成立：线性 [0,1] 融合的有效权重既不等于名义权重（π₁ = 0.532 对 w₁ = 0.40），也随单调重参数化而漂移（非线性单调下 L1 = 0.1276、36.4% 组合排序反转、最强反例完全反号），而分位 logit 公制对其严格不变（同校准 0、独立校准 0.016 抽样噪声）；在 assist09 的 994 道题上，真实漂移由 0.5357 降至 0.0001。多信号融合确实提升了与专家难度标签的一致性（DBE 0.2207 → 0.2899，重复交叉验证优化权重 0.5240 ± 0.1069；Junyi 0.2610 → 0.3629，重复交叉验证 0.4023 ± 0.0480），其驱动力是一个"自报难度高且信任度低"的差分特征；但专家标签与真实表现难度的秩相关只有 0.4326，判据换成留出表现后融合在两个数据集、全部样本量档上均劣于仅用成功率，胜率 0.00——这些增益是一致性增益，不是预测力提升。本文进一步定位到融合配置中的一处方向性错误并提出样本量自适应收缩，其收益在 k = 10 时达 +3.43 个百分点（O8 口径）、在 k ≥ 100 时衰减至 0.22 个百分点以下，且未在 DBE 上复现。对融合估计器本身的进一步优化（符号校正 + 折半信度加权，`srw7`）已在记录级与学生级留出上双重验证：在全部样本量档上均不劣于仅成功率，且一致优于只做剔除的 fused6。最后，本文报告 assist09 上滑窗错误率的众数区间为 0.15–0.20（成功率 80–85%），并明确说明这一描述性证据无法在 85% 规则与 80.7% 之间作判别；原预注册的剂量—反应识别未执行，本文因而不作任何关于最优错误率的因果断言。三项结论已部分落地到被测系统的难度公制层（全量测试 934 条通过）；`srw7` 估计器亦已作为增量函数 `estimate_optimized_difficulty()` 落地到 `difficulty_fusion.py`，其正确性由外部校验脚本保证（第 9.2 节）。

---

## 参考文献

[1] Wilson, R. C., Shenhav, A., Straccia, M., & Cohen, J. D. (2019). The Eighty Five Percent Rule for optimal learning. *Nature Communications*, 10(1), 4646. DOI: 10.1038/s41467-019-12552-4.

[2] Baillifard, A., Belardi, A., & Martarelli, C. S. (2025). Engagement beyond success rate: Evidence from an adaptive learning system. （出版方、卷期与 DOI/ISBN **待核验**；n = 413；最优成功率约 80.7%）

[3] Guadagnoli, M. A., & Lee, T. D. (2004). Challenge point: A framework for conceptualizing the effects of various practice conditions in motor learning. *Journal of Motor Behavior*, 36(2), 212–224. （DOI 待核验）

[4] Hodges, N. J., & Lohse, K. R. (2022). An extended challenge-based framework for practice design in sports coaching. *International Review of Sport and Exercise Psychology*. （卷期/DOI 待核验）

[5] Weber, D., Becker, N., Spinath, F. M., & Koch, M. (2025). The stability of IRT parameters under several test equating conditions. *Frontiers in Psychology*, 16, 1652341. DOI: 10.3389/fpsyg.2025.1652341.

[6] Deng, J. (2025). Linking errors introduced by rapid guessing responses when employing multigroup concurrent IRT scaling. *Large-scale Assessments in Education*, 13, 28. DOI: 10.1186/s40536-025-00265-8.

[7] Liu, Z., Guo, T., Liang, Q., Hou, M., Zhan, B., Tang, J., Luo, W., & Weng, J. (2025). Deep learning based knowledge tracing: A review, a tool and empirical studies. *IEEE Transactions on Knowledge and Data Engineering*, 37(8), 4512–4536. DOI: 10.1109/TKDE.2025.3552759.

[8] Ding, M., Agrawal, A., Choo, J., Deng, C., et al. (2024). Easy2Hard-Bench: Standardized difficulty labels for profiling LLM performance and generalization. *Advances in Neural Information Processing Systems (NeurIPS), Datasets and Benchmarks Track*. arXiv:2409.18433.

[9] Dudík, M., Langford, J., & Li, L. (2011). Doubly robust policy evaluation and learning. *Proceedings of the 28th International Conference on Machine Learning (ICML)*.

[10] Chernozhukov, V., Chetverikov, D., Demirer, M., Duflo, E., Hansen, C., Newey, W., & Robins, J. (2018). Double/debiased machine learning for treatment and structural parameters. *The Econometrics Journal*, 21(1), C1–C68. DOI: 10.1111/ectj.12097.

[11] VanderWeele, T. J., & Ding, P. (2017). Sensitivity analysis in observational research: Introducing the E-value. *Annals of Internal Medicine*, 167(4), 268–274. DOI: 10.7326/M16-2607.

[12] Feng, M., Heffernan, N. T., & Koedinger, K. R. (2009). Addressing the assessment challenge with an online system that tutors as it assesses. *User Modeling and User-Adapted Interaction*, 19(3), 243–266. （DOI 待核验；ASSISTments 2009–2010 skill-builder 数据页 URL 待核验）

[13] Abdelrahman, G., Wang, Q., et al. (2022). Knowledge tracing for complex skill training data. *arXiv:2208.12651*. （DBE-KT22；ADA Dataverse DOI: 10.26193/6DZWOH）

[14] Liu, Z., et al. (2023). XES3G5M: A knowledge tracing benchmark dataset. *Advances in Neural Information Processing Systems (NeurIPS), Datasets and Benchmarks Track*. （条目与页码待核验；MIT 许可）

[15] Chang, H.-S., Hsu, H.-J., & Chen, K.-T. (2015). Modeling exercise relationships in e-learning: A unified approach. *Educational Technology & Society*, 18(2), 128–141. （卷期待核验；Junyi Academy 数据集，CC-BY-NC-SA-4.0，禁商用）

[16] Kolen, M. J., & Brennan, R. L. (2014). *Test Equating, Scaling, and Linking: Methods and Practices* (3rd ed.). Springer.

[17] Platt, J. (1999). Probabilistic outputs for support vector machines and comparisons to regularized likelihood methods. In *Advances in Large Margin Classifiers*. MIT Press.

[18] Guo, C., Pleiss, G., Sun, Y., & Weinberger, K. Q. (2017). On calibration of modern neural networks. *Proceedings of the 34th International Conference on Machine Learning (ICML)*.

[19] Efron, B., & Tibshirani, R. J. (1993). *An Introduction to the Bootstrap*. Chapman & Hall.

[20] Cohen, J. (1968). Weighted kappa: Nominal scale agreement with provision for scaled disagreement or partial credit. *Psychological Bulletin*, 70(4), 213–220.

[21] Benjamini, Y., & Hochberg, Y. (1995). Controlling the false discovery rate: A practical and powerful approach to multiple testing. *Journal of the Royal Statistical Society: Series B*, 57(1), 289–300.

[22] Atroszko, P. A., Charzyńska, E., Buźniak, A., Czerwiński, S. K., Griffiths, M. D., et al. (2025). Validity, reliability, and cross-cultural comparability of a problematic overstudying scale. *International Journal of Mental Health and Addiction*. DOI: 10.1007/s11469-023-01128-5.

[23] Loscalzo, Y., Wetstone, H., Schuldberg, D., Giannini, M., & Rice, K. G. (2024). Studyholism in the United States and Italy. *Current Psychology*, 43(29), 24608–24621. DOI: 10.1007/s12144-024-06163-6.

[24] 吴艳, 戴晓阳, 温忠麟, 崔汉卿. (2010). 青少年学习倦怠量表的编制. *中国临床心理学杂志*, 18(2), 152–154.

---

## 投稿前须核验清单

| # | 核验项 | 类型 | 处置 |
|---|---|---|---|
| 1 | 四个数据集的许可与引用义务（assist09 / DBE-KT22 / XES3G5M / Junyi，含 Junyi 禁商用） | **阻断级** | 逐条核验官方页面后方可投稿 |
| 2 | 全量测试数量统一写 **934**（"920"仅可作为"改动前数值"并注明） | **数字一致** | 已执行；全文复核 |
| 3 | O1 只引用 **0.5357** 口径；被更正的旧数值仅以"更高"定性出现于口径更正记录，不写出且不作结果引用 | **数字一致** | 已执行；全文复核 |
| 4 | O4 / O5 / O6 增益一律表述为"与专家标签的一致性增益"，**不得**表述为"预测力提升" | **诚信红线** | 已执行；第 6.3 节与第 7.2 节双重声明 |
| 5 | 优化权重优先引用重复 5 折 ×10 值（DBE 0.5240 ± 0.1069；Junyi 0.4023 ± 0.0480）；单次 5 折的 0.4778 / 0.3837 须注明口径 | **数字一致** | 已执行 |
| 6 | 正文全部数字与 `results/code/` 下脚本产物一致；不复算、不重估、不补齐未执行实验 | **数字一致** | 以 JSON 原始产物为准 |
| 7 | 合成实验（第 4 节）须显式标注"合成数据"及其最有利环境性质 | **诚信红线** | 已执行（§4.1 / §4.6） |
| 8 | 因果侧（H4–H7 及 AIPW/DR、非单调性、五前提分解、E-value）如实标注"未执行"，全文无因果断言 | **诚信红线** | 已执行（§1.3 / §8.3 / §8.4 / §10） |
| 9 | LAI 相关表述均标注"未验证 / 本文未使用 / 无临床暗示" | **诚信红线** | 已执行（§9.3 第七条） |
| 10 | 系统计数口径：机制 **54**、学习方法 **28**、指纹 `ee1a49be5732`；不出现虚高计数 | **数字纪律** | 已执行（文首声明二） |
| 11 | Baillifard 等（2025）出版方与 DOI；Guadagnoli & Lee（2004）DOI；Hodges & Lohse（2022）卷期；Feng 等（2009）DOI；XES3G5M 与 Chang 等（2015）条目 | **引用合规** | CrossRef / OpenAlex 逐条核对 |
| 12 | 目标期刊对"负结果 / 部分未完成研究"的收稿政策与字数要求 | 投稿合规 | 以官网征文为准 |
| 13 | 目标期刊最新 AIGC 检测与数据可用性声明政策 | 投稿合规 | 以编辑部公告为准 |

---

*本稿为 M1 完整稿，由 `M1_难度可公度性与最优错误率_预注册设计.md` 改写而成；原预注册设计稿保留留档、本次未作任何改动。全部数字取自 `results\Real_data_实验报告.md`（§二、§六–§十）与 `results\M1_instability.md`，未作任何复算、估算或补齐。系统口径严格遵守：游戏化干预机制 54 个（LF-M01…LF-M54）、学习方法 28 种、注册表指纹 ee1a49be5732、后端全量测试 934 条通过（改动前 920）。引用均来自真实检索文献，标注"待核验"者投稿前必须闭环。*
