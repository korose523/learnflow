# LearnFlow 文档重写统一规范（权威事实基线 + 学术写作体例）

> 本文件是本次全项目文档重写的**唯一规范源**。所有参与重写的作者必须先通读本文件，严格执行。
> 版本：2026-09-13 · 代码 HEAD = `4073bb0`（O8 融合估计器落地版本；count_verification.json 于 2026-09-13 复算，六项全部 verified，机制数不变仍为 54）。
> 本次校准说明：原表"测试 868 / 49 文件"与"后端 81 文件 / 23,927 行"为 2026-09-10 快照，已按 2026-09-13 实测更新为 934 / 54 文件与 88 文件 / 25,795 行；services 文件数由 54 更新为 55。

---

## 第一部分 · 权威事实基线（覆盖一切历史数字）

以下每个数字都有对应的复算命令。**任何文档中出现与本表冲突的数字即为不合格**。

| 指标 | 权威值 | 复算命令（在 `E:\learnflow\learnflow-backend` 下执行） |
|---|---:|---|
| 唯一游戏化机制数 | **54**（LF-M01…LF-M54） | `python scripts/verify_counts.py` → `artifacts/count_verification.json` |
| 机制分类（八类 A–H） | 8 / 8 / 6 / 10 / 10 / 4 / 4 / 4，和 = 54 | 同上（`category_counts`） |
| 机制注册表指纹 | `ee1a49be5732`（`registry_fingerprint()`，`app/services/mechanism_registry.py:145`） | `python -c "from app.services.mechanism_registry import registry_fingerprint as f; print(f())"` |
| 机制运行时落地 | **54 / 54，orphan = 0** | `python scripts/scan_mechanism_landing.py --quiet` → `landed=54/54 orphan=0` |
| 学习方法数 | **28**（LF-L01…LF-L28，`app/services/method_registry.py`） | `python scripts/verify_counts.py` |
| 元学习技能树节点 | **16**（`SKILL_DEFINITIONS`） | 同上 |
| 实现单元数（informative，非论文主表述） | 60（mechanism_units） | 同上（`mechanism_units`） |
| 引擎类数（informative） | 86（engine_classes） | 同上（`engine_classes`） |
| PRD §2.3 表格逐项求和 | **69** | 同上（`prd_claimed`） |
| PRD 标题/概述声称值 | 76（**与表格自相矛盾**） | 同上（`prd_headline_claimed`） |
| 后端 Python | 88 个文件 / 25,795 行 | `python -c "import os;ps=[os.path.join(d,f) for d,_,fs in os.walk(\"app\") for f in fs if f.endswith(\".py\")];print(len(ps),sum(len(open(x,encoding=\"utf-8\").readlines()) for x in ps))"` |
| 测试 | 54 个测试文件 / **934 passed**（0 error / 0 failure / 16 warning） | `python -m pytest tests/ -q`（实测 2026-09-13：`934 passed, 16 warnings in 104.55s`） |
| 可复现性印章 | `artifacts/count_verification.json`（6 项全部 `verified`；HEAD = 4073bb0） | `python scripts/verify_counts.py` |
| 预注册实验设计 | `scripts/prereg/ablation_design_v1.yaml`（DEFF = 2.45，分层 Gatemaking Gate0/1/2/2.5） | 直接读文件 |
| 埋点 | `learning_events` 表 + `decision_snapshot` JSON（`record_learning_event`） | `app/services/progression_repository.py` |
| 机制编排 | `PIPELINE_MECHANISM_MAP`：step 6 / 8 / 9 / 12 为主机制，**step 13 为 28 个次级机制评估钩子** | `app/services/learning_orchestrator.py` |
| 干预仲裁 | `MechanismArbitrator` 三层漏斗（健康一票否决 → 冲突消解 → 干预预算） | `app/services/mechanism_arbitrator.py` |
| 实验框架 | `ab_test_framework.py`；`SQLExperimentStore` 已落地（SQLAlchemy Core，方言无关）；`--off-category` 类别级消融；`--advance-to` 尊重 `required_n_per_group` | `scripts/run_experiment.py` |

### 禁用数字清单（出现即判定不合格）

| 禁用表述 | 正确表述 |
|---|---|
| 76 个/种机制 | 54 个唯一机制（LF-M01…LF-M54）；若必须讨论 PRD 的 76，须明确写成"PRD 标题声称 76，与其 §2.3 表格求和 69 自相矛盾，经三层复算唯一机制为 54" |
| 23 / 21 种学习方法 | 28 种学习方法（LF-L01…LF-L28，权威源 `method_registry.py`） |
| 48 / 53 个零调用机制 | 机制运行时落地 54/54，orphan = 0（2026-09-10 完成接线；`LF-M52` MinorProtectionEngine 现有 16 处引用） |
| 394 / 685 / 868 / 920 个测试 | 934 passed（54 个测试文件） |
| 60 个机制（作为主表述） | 60 是实现单元计数（informative），论文主表述一律用 54 |
| 后端 16,593 / 20,349 / 23,927 行 | 25,795 行（88 个文件） |

### 算法结果基线（O1–O12，2026-09-13 算法优化后的权威数值）

以下数值是 M1/M3 两篇论文中一切算法论断的**唯一**数值来源。凡引用留出增益、收缩系数、
融合变体比较、显著性检验的段落，必须与本节一致；与本节冲突即为不合格。

| 项 | 权威值 | 证据 |
|---|---|---|
| O1 难度可公度性系数（权重漂移 L1，ECDF→logit 分位链接） | **0.5357**（**禁止**写作 0.76） | `results/code/o1_*.json` |
| O7 留出协议 | 拟合块 A（k 个蓄水池槽位）与判据块 B（250，互斥）；`difficulty_cache.npz`（seed 20260913，K=500） | `results/code/build_difficulty_cache.py` |
| O8 记录级融合列（fused6） | 去掉 `upgrade_rate`，`FUSED6_COLUMNS=(0,1,2,3,5,6)` | `results/code/o8_confirm_closedform.py` |
| O8 闭式收缩系数 | `λ_cf(k)=1/(1+(k/27.3)^0.895)`；k=10/25/50/100/200 → 0.711/0.520/0.368/0.238/0.144 | `results/code/o9_lambda_generalization.json` |
| O11 优化估计器 | 符号校正 + 折半信度加权融合 `srw7`；符号/权重/λ **全部只在拟合块 A 内估计**（无泄漏） | `results/code/o11_fusion_optimization.py` |
| O11 复现规模 | k=10/25/50/100/200，REP=25，SPLITS=20，B=250 | 同上 |
| O12 学生级留出 | O10 协议（用户按 `crc32(uuid)` 奇偶二分，推定≥500 行）、1,238 题、蓄水池/半=250、REP=25×SPLITS=20 | `results/code/o12_student_o11.py` |
| 落地位置 | `app/services/difficulty_fusion.py::estimate_optimized_difficulty`（定义于 `:278`；该文件共 338 行） | `results/code/o11_land_verify.py`（15/15 PASS） |

**表 A-1 记录级留出增益（相对"仅成功率"基线，单位 pp，λ=λ_cf）**

| k | 仅成功率(held-out) | fused6 | fused7 | signed7 | rw6 | **srw7** | λ_cf | λ_rel | λ*_emp |
|---|---|---|---|---|---|---|---|---|---|
| 10 | 0.6698 | +3.278 | +1.498 | +3.594 | +3.616 | **+3.823** | 0.7107 | 0.7010 | 0.7000 |
| 25 | 0.8060 | +1.586 | +0.523 | +1.930 | +1.829 | **+2.025** | 0.5197 | 0.4902 | 0.5270 |
| 50 | 0.8824 | +0.659 | +0.181 | +0.850 | +0.806 | **+0.930** | 0.3678 | 0.3136 | 0.3723 |
| 100 | 0.9248 | +0.243 | +0.073 | +0.327 | +0.335 | **+0.391** | 0.2383 | 0.1881 | 0.2370 |
| 200 | 0.9502 | +0.071 | +0.013 | +0.111 | +0.108 | **+0.138** | 0.1440 | 0.1041 | 0.1333 |

> 复算命令：`python results/code/o11_fusion_optimization.py`（输出 `results/code/o11_fusion_optimization.json`）

**表 A-2 学生级留出增益（O12，单位 pp）**

| k | 仅成功率 | fused6 | **srw7(λ_cf)** | srw7−fused6 | 胜率 | Wilcoxon p |
|---|---|---|---|---|---|---|
| 10 | 0.6631 | +2.390 | **+4.283** | +0.662 | 0.902 | 1.15e-66 |
| 25 | 0.8133 | **−1.402** | **+2.189** | +0.557 | 1.000 | 1.26e-83 |
| 50 | 0.8881 | **−3.905** | **+0.951** | +0.317 | 1.000 | 1.26e-83 |
| 100 | 0.9356 | **−5.728** | **+0.335** | +0.169 | 0.998 | 1.27e-83 |
| 200 | 0.9603 | **−6.804** | **+0.102** | +0.081 | 0.996 | 1.29e-83 |

> 复算命令：`python results/code/o12_student_o11.py`（输出 `results/code/o12_student_o11.json`）
> **必须如实陈述的否定性结论**：在学生级留出下，O8 的 `fused6` 估计器在 k≥25 时**低于"仅成功率"基线**
> （−1.402 / −3.905 / −5.728 / −6.804 pp），即该简化估计器在样本量增大后有害；
> O11 的 `srw7` 是同协议下唯一在所有 k 上均不劣于基线且优于 fused6 的变体。不得隐去该反向结果。

**三处易错口径（复用时必须遵守）**

1. **λ 三种取法的差距不是 ≤0.02**：实测最大 0.0587（k = 50，`λ_rel` 0.3136 对 `λ*_emp` 0.3723）；`λ_cf` 与 `λ*_emp` 的最大差为 0.011。正确表述："λ 取值最大相差 0.059、三者带入 `srw7` 后的留出增益相差 ≤0.087 pp"。
2. **`srw7 − fused6` 列是配对口径**，不能由相邻两列相减复核：该列为同一 λ_cf 下、500 个配对折半上 `srw7` 与 `fused6` 留出 ρ 之差的均值，而 `fused6` 列为未叠加 λ 的原始 ρ、`srw7(λ_cf)` 列为已叠加 λ 的 ρ（k = 10：相减得 1.893 pp，配对值为 0.662 pp）。
3. **复算 o11/o12 需 numpy**：项目 venv（`learnflow-backend/.venv`，Python 3.11.9）**未装 numpy**，直接运行会 `ModuleNotFoundError`；本轮实测由 Python 3.13 + numpy 2.5.3 通过。落地校验脚本 `o11_land_verify.py` 同样需要 numpy。
4. **一键对账工具**：`python results/code/reconcile_paper_tables.py` —— 把 M1 表 7-1 / O10 表 与 M3 表 11 / 表 10 / 表 12 对到 `o10_user_holdout.json`、`o11_fusion_optimization.json`、`o12_student_o11.json`，逐值比对、不一致即退 1；不依赖 numpy，已实测 **ALL PASS（30 行）**。

### 必须如实披露的未实现项与已知缺陷（不得隐藏）

1. `app/services/state_store.py`：`StateStore` 接口已落地（9 个内存态容器迁移完成），但默认仍为 `MemoryStateStore`（含 `default_state_store`），**未落盘持久化**，进程重启即丢（与改造前行为一致）；
2. 4 个机制（`app/services/mechanism_registry.py`：`LF-M29` 礼物经济 `social_addiction_engine.py:411`、`LF-M32` 家长门户 `social_addiction_engine.py:288`、`LF-M49` 空间锚定 `ux_addiction_engine.py:290`、`LF-M50` 多感官包装 `ux_addiction_engine.py:345,482,524`）`maturity = "placeholder"`，其 step-13 输出为引擎占位数据；
3. （已解决·留痕）`LF-M52` `MinorProtectionEngine` 已于 2026-09-10 接线，现有 16 处引用（含编排器健康门控），不再为零调用；`SQLExperimentStore`（`ab_test_framework.py:510`）已落地真实 SQLAlchemy Core 后端（方言无关，跨重启持久化）；技能树持久化 `_skilltree_repo_format`（`learning_orchestrator.py:430`）已兼容 `get_skill_tree`（`meta_learning_skilltree.py:273`）返回的 `categories` 形状，原 `AttributeError` 路径已消除。

---

## 第二部分 · 学术写作体例（期刊论文 / 博士论文统一标准）

### 2.1 语言与语域

- 中文学术书面语，**禁止**博客/口语/营销表达：不得出现"结论先行""一句话结论""血泪建议""诚实说""最锋利的""翻车""坑"等；
- **禁止**使用 emoji（⭐ 🔴 ✅ ⚠️ 等）作为语义载体；
- 判断性内容**保留**，但改用学术措辞（例："这个数字不成立" → "该数值与可复算实测值不符，差异来源见表 X"）；
- 首次出现的术语须给出英文原文并下操作化定义（例："心流通道（flow channel）……本文将其形式化为……"）。

### 2.2 结构

- 三级编号：`1` / `1.1` / `1.1.1`；
- 每章开头写**本章摘要**（80–150 字），章末写**本章小结**（3–5 条要点）；
- 全文开头写**摘要**（200–300 字）+ **关键词**（4–6 个）；
- 文档末尾视需要设**附录**（机制全表、复算命令、符号表）。

### 2.3 主张—证据耦合（本项目最强约束）

每个实证性论断之后必须标注证据类型之一：

- **代码证据**：`app/services/xxx.py:123`（行号必须真实，写前请核实）；
- **复算证据**：标注命令，如「复算命令：`python scripts/verify_counts.py`」；
- **文献证据**：`[@citekey]`。

**禁止**无源断言："研究表明""众所周知""显然""学习科学表明""业界公认"等一律删除或补源。

### 2.4 引用规范

- 统一 `[@citekey]` 形式；
- 键必须存在于 `E:\learnflow\docs\references.bib`；
- **严禁凭记忆编造文献**（作者、年份、期刊、DOI）。若确实需要而 bib 中没有，写 `[@待核验:作者年份]` 并在文末「待核验引用清单」中单列；
- 参考文献表按作者—年份排序，中文文献在后。

### 2.5 图表规范

- 表/图必须编号 + 标题：**表 3-2 机制分类学一览**、**图 4-1 三层漏斗仲裁流程**；
- 正文必须显式引用（"如表 3-2 所示……"），不得出现孤立图表；
- 表格一律用 Markdown 管道语法；
- 任何含数字的表格下方加一行：> 复算命令：`…`（输出：`artifacts/xxx.json`）。

### 2.6 篇幅与诚实性

- 重写后篇幅**不得少于原文档**（原行数由主理人在任务书中给出）；
- 局限、效度威胁（threats to validity）、否定性结论必须单列小节如实陈述；
- 不得通过删减内容来"达标"；不得为让数字好看而调整口径。

### 2.7 交付要求

- 只写文件，**不要执行 git commit**（由主理人统一提交）；
- 完成后向主理人报告：① 文件路径与新行数；② 修正的陈旧数字清单（旧 → 新）；③ 新增的「待核验引用清单」；④ 未能核实的事项。
