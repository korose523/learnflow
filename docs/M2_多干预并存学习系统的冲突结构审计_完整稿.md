# 多干预并存学习系统的冲突结构审计

## ——基于 54 个游戏化干预机制的静态审计、跨系统复现与一组负结果

**English Title:** Auditing the Conflict Structure of Multi-Intervention Learning Systems: A Static Audit of 54 Gamified Mechanisms, Cross-System Replication, and Negative Results

> **目标中文期刊**：《软件学报》（CCF-A 中文、EI、CSCD；收稿范围含"系统软件与软件工程"；不收版面费；基金非强制）。
> **备选期刊**：《计算机科学》（CCF-B 中文，设"人机交互/普适计算"栏目）。
> **学科外壳**：软件工程 / 实证软件审计（静态审计 + 跨系统复现 + 负结果报告）。
> **本文的三条"不宣称"声明**：① 不宣称任何游戏化机制对学习有效；② 不作单机制消融实验；③ **不声称已完成干预账本一致性实验**——仓库中尚无账本实现，本文只把账本作为由审计导出的设计需求。
> **数据与代码可得性**：本文全部数字由只读运行审计脚本产生，产物见 `results/m2/`（含 `M2_results.md` 报告的逐条复算命令）；被审计仓库为 `E:\learnflow`，代码基线 commit `8a328430d36860043f3bf80e8e0bf2a82edd2ecd`（2026-09-11）。审计过程未修改被审计仓库任何文件（运行前后 `git status --short` 逐行一致）。

---

## 摘要

当一个学习系统同时登记数十个行为干预机制时，直觉预期是这些机制会在运行时相互冲突、彼此抵消，因而需要仲裁与预算治理。本文对这一直觉做了正面检验，结果与直觉相反。本文对研究依托的开源自适应学习平台作静态审计——该平台以统一机制注册表登记 **54 个**语义去重后的游戏化干预机制（LF-M01…LF-M54），并报出四点实测结果。第一，把机制映射到本文定义的干预七元组时，54 个机制中有 **42 个（77.8%）无法被赋予方向字段**，而 `target_construct`、`channel`、`side_effect`、`precedence` 四个字段在代码中根本不存在——这**触发了本文事前设定的证伪条件（不可归类比例 > 20%）**，据此得出的结论是 schema 粒度须重设，而非"覆盖率良好"。第二，三类冲突中，类型 I 的静态潜在对为 27 对、文档锚定的同构念对为 2 对，但**运行时实际可检出的只有 1 对**（LF-M44 与 LF-M52），且其消解发生在"健康一票否决"层而非本文设计的"方向消解"层——后者在当前干预效果生产者集合下是**死代码**。第三，类型 II（正名为**预算竞争**）的运行时竞争对数为 **0**，结构性原因是次级机制只写评估记录、不构造干预效果、不进入仲裁器、不消耗预算。第四，跨系统复现（Ludilearn 6 机制、Level Up XP 11 机制）显示：在严格口径下两个外部系统的类型 I 冲突均为 **0**，它们用互斥激活与速率窗口把冲突**按构造规避**，而不是解决。本文由此给出的主线结论是一个负结果：**冲突在多干预并存架构中才暴露，而多数同类系统通过结构性设计回避了冲突本身**；因此"54 机制并存"这一架构是稀缺样本，而不是普遍困境。本文同时如实报告审计方法自身的失败之处（schema 覆盖不足、账本尚未实现、外部系统编码未算评分者信度），并给出由审计导出的设计需求。

**关键词**：游戏化干预；冲突审计；实证软件审计；干预注册表；预算竞争；负结果

## Abstract

When a learning system registers dozens of behavioural intervention mechanisms at once, the intuitive expectation is that they will collide at run time, cancel each other out, and therefore require arbitration and budget governance. This paper tests that intuition head-on, and the results run against it. We perform a static audit of the open-source adaptive learning platform on which this research is based — a platform whose unified mechanism registry holds **54** semantically de-duplicated gamified intervention mechanisms (LF-M01…LF-M54) — and report four measured findings. First, when mechanisms are mapped onto the intervention seven-tuple defined here, **42 of 54 (77.8%) cannot be assigned a direction field**, and four fields (`target_construct`, `channel`, `side_effect`, `precedence`) do not exist in the code at all; this **triggers the falsification condition we set in advance (unclassifiable share > 20%)**, so the conclusion is that the schema's granularity must be reset, not that coverage is adequate. Second, of the three conflict types, type I has 27 static latent pairs and 2 document-anchored same-construct pairs, yet only **one pair is detectable at run time** (LF-M44 vs LF-M52), and it is dissolved at the "health override" layer rather than at the "direction cancellation" layer we designed — the latter is **dead code** under the current set of intervention-effect producers. Third, type II — renamed here as **budget contention** — exhibits **zero** run-time contending pairs, because secondary mechanisms only write evaluation records: they construct no effect, never enter the arbitrator, and consume no budget. Fourth, cross-system replication (Ludilearn with 6 mechanisms; Level Up XP with 11) shows that under the strict criterion both external systems yield **zero** type I conflicts: they **structurally avoid** conflict by mutually exclusive activation and rate windows rather than resolving it. Our headline conclusion is thus a negative result: **conflict surfaces only in multi-intervention architectures, and most peer systems sidestep the conflict itself**; a 54-mechanism architecture is therefore a scarce specimen rather than a common predicament. We also report, without flinching, where our own audit method failed (insufficient schema coverage, an unimplemented ledger, no inter-rater reliability for external coding), and we derive design requirements from the audit.

**Keywords**: gamified intervention; conflict audit; empirical software audit; intervention registry; budget contention; negative results

---

## 1 引言

**本章摘要**：本章交代本文的问题来源。自适应游戏化研究长期以"打包的游戏化 vs 无游戏化"为比较单元，未能回答多个干预机制同时在场时彼此会发生什么。本章指出现有工作在该问题上的空白，说明审计对象存在的三处内部矛盾——分类学双轨、语义对立与运行时行为的落差、机制计数的多口径，并交代本文的四项贡献与全文结构。

### 1.1 一个工程事实，与一个未被正面命名的问题

游戏化学习系统在过去十五年里经历了从"徽章—积分—排行榜"三件套到"自适应游戏化"的演进。后者的核心主张是：不同用户偏好不同的游戏元素，系统应根据用户画像动态选择元素。与之呼应，大量研究围绕用户类型学（如 HEXAD）与元素偏好展开；近期元分析也确认了游戏化的平均效应确实存在——Dai、Xu 与 Xing（2025）在 182 个效应量、37 项随机对照试验上报告合并效应量 d = 0.566，且"规则/目标 + 挑战 + 神秘感"的组合最强。

然而这些效应量都建立在同一个比较之上：**打包的游戏化 vs 无游戏化**。它们无法回答一个在工程上早已出现、在研究上却未被正面命名的问题：当系统内同时登记并调度数十个干预机制时，这些机制**彼此之间**会发生什么？直觉的回答是：它们会冲突——有的要延长在线时长，有的要限制在线时长；有的争用同一条提示通道，有的争抢同一份干预预算。若这一直觉成立，多机制系统就必然需要冲突检测、仲裁与预算治理，而这些能力在现有文献与工业系统中都付之阙如。

本文要做的事情，就是把这一直觉当作一个**可被证伪的经验命题**来检验，而不是当作论证前提。检验的结论与直觉不一致，这个不一致本身构成了本文的主要内容。

### 1.2 审计对象的既有矛盾

本文的审计对象是一个真实建成、并已公开的 K12 自适应学习平台。它的一个显著特征是规模：以统一机制注册表登记 **54 个语义去重后的游戏化干预机制**（编号 LF-M01…LF-M54，连续无缺口），注册表指纹 `ee1a49be5732`。这一规模使"多干预并存"从一句设计口号变成了可以被逐行检查的工程事实。

正因如此，该对象提供了三处**可利用的内部矛盾**，它们既是审计的入口，也是审计价值的来源。

其一是**分类学的双轨**。治理文档以 A–H 八个类别组织这 54 个机制（A 8 / B 8 / C 6 / D 10 / E 10 / F 4 / G 4 / H 4）。而代码侧 `CATEGORIES` 常量只有**五个**取值（retention 19 / motivation 18 / selfreg 9 / cognition 4 / health 4）。两套分类学并不互为精化关系，这一点在本文第 4 节被逐字母量化。

其二是**语义对立与运行时行为的落差**。系统里确实存在语义上直接对立的机制：错失恐惧（FOMO）提示以延长在线时长为目标，方向为"促进"；未成年人时长保护以限制时长为目标，方向为"抑制"且带合规强制性；休息提醒同样以缩短连续使用为目标。若只看文档，这是"系统同时鼓励与劝阻同一行为"的设计矛盾；但只看文档并不能回答它是否真的会在运行时发生——而后者才是软件审计要回答的问题。

其三是**数字的多口径**。同一份产品需求文档（PRD）内部，§2.3 表格的逐项求和为 69，而该文档的标题与概述写作 76；两者不一致（内部标记为 `internally_inconsistent = true`）。一个连自身计数都无法自洽的文档，恰好说明"在缺乏注册表与指纹机制时，机制计数无法被稳定复现"。

### 1.3 本文的贡献与主要发现

本文是一次**静态审计 + 跨系统复现**，不提出新的检测算法，也不声称任何机制对学习有效。贡献可归纳为四项，每项都由实测数字支撑。

**贡献一：把"干预冲突"从设计词汇改造为可逐行检查的审计判据，并诚实报告该判据在当前系统上的覆盖率失败。** 本文将干预形式化为七元组《trigger, target_construct, direction, channel, cost, side_effect, precedence》，并逐字段检查其可得性。实测结果是：只有 `trigger`（即 `stage`，54/54 可得）与 `direction`（12/54 可得）存在，其余四个字段在代码中不存在；**不可归类比例高达 42/54 = 77.8%**。这一数字触发了本文在审计前设定的证伪条件（> 20%），因此本文的自我判定是"schema 粒度须重设"，而非"映射成功"。

**贡献二：给出三类冲突在本系统注册表内的实际检出，并区分"静态潜在"与"运行时实际"。** 类型 I 的静态潜在对为 27（9 个 approach × 3 个 withdraw），文档锚定的同构念对为 2；但**运行时实际可检出仅 1 对**。类型 II（预算竞争）运行时竞争对数为 **0**。类型 III（schema 归类冲突）表现为治理文档 8 个字母中有 **7 个**横跨 ≥2 个注册表类别，表明两套分类学不互为精化。

**贡献三：定位到一个可直接举证的软件工程发现——第 2 层的方向消解在当前干预效果生产者集合下是死代码。** 全仓库只有两处构造干预效果（`Effect`）：`deep_addiction_engine.py:296` 的 LF-M44（`user_visible=True, health_critical=False`）与 `learning_orchestrator.py:986` 的 LF-M52（`user_visible=False, health_critical=True`）。而方向消解只在 `user_visible` 的效果之间比较方向，故 LF-M52 从不进入方向比较；语义对立实际由**第 1 层健康一票否决**消解。换言之，本系统实现的是"健康否决"语义，而非"方向消解"语义。

**贡献四：把单点观察升级为跨系统比较，并得到一个负结果为主导的结论。** 对两个外部游戏化系统（Ludilearn、Level Up XP）的复现显示：严格口径下类型 I 冲突均为 0。Ludilearn 通过"同一 section 同一时刻只有一个游戏元素"的互斥激活使冲突按构造不存在；Level Up XP 则用 H/D/W/M 四级速率窗口实现了对"干预预算"的独立治理。**冲突在这两个系统里不是被解决了，而是被结构性规避了。** 这反向支持本文的问题定位：多机制并存架构才是让冲突暴露的稀缺条件。

### 1.4 结构与可复现性

第 2 节梳理相关工作；第 3 节给出干预形式化判据与三类冲突的实测检出，并报告死代码发现与合规抢占性质 S1 的审计结论；第 4 节报告机制普查、两套分类学的关系与数字漂移；第 5 节把账本从"已评估贡献"降级为"由审计导出的设计需求"；第 6 节报告跨系统复现（E2）；第 7 节以"预测 vs 实测"并排的形式给出检验对照；第 8 节讨论；第 9 节列出诚实缺口；第 10 节结论。

**可复现性说明。** 本文全部数字由只读运行审计脚本产生：由于 `learnflow-backend/artifacts/` 是被 git 跟踪的目录，直接运行仓库脚本会覆写被跟踪文件，因此审计通过一个只读运行器（`results/m2/harness.py`）以 importlib 加载脚本模块、把模块级输出路径常量重定向到 `results/m2/` 后调用其 `main()`——跑的是脚本本体逻辑，但不写仓库。运行前后 `git status --short` 输出逐行一致。为让"预测—实测"对照不受事后调整影响，本文的分析方案在审计执行前即已固化并单独存档。

### 1.5 本章小结

1. 研究问题：多干预并存系统中机制之间的相互作用，此前未被正面命名，也未被形式化为可审计的判据。
2. 审计对象：一个 K12 自适应学习平台，以统一注册表登记 **54 个**语义去重后的游戏化干预机制（LF-M01…LF-M54，连续无缺口），注册表指纹 `ee1a49be5732`。
3. 该对象提供了三处可利用的内部矛盾（分类学双轨、语义对立与运行时落差、PRD 计数多口径），它们既是审计的入口，也是审计价值的来源。
4. 本文是静态审计 + 跨系统复现，不提出新的检测算法，也不宣称任何机制对学习有效。
5. 可复现性：全部数字由只读运行器（`results/m2/harness.py`）产生，审计前后 `git status --short` 逐行一致。

---

## 2 相关工作

**本章摘要**：本章梳理四条相关工作脉络：自适应游戏化与用户类型学、行为改变科学与 JITAI、多目标辨识与预算约束在线学习、游戏化效果基线与暗面证据。本章的重点不在综述本身，而在于指出这四条脉络共同的结构性缺口——它们把干预视为静态选项或彼此独立的成分，未把"多个干预同时在场"当作可审计的对象。该缺口正是本文审计判据的出发点。

### 2.1 自适应游戏化与用户类型学

自适应游戏化的主流范式以用户类型学为轴。Sailer 等（2017）提出 HEXAD 类型学，把用户区分为成就者、自由者、社交者、慈善者、玩家与 disruptor；后续元分析（Sailer 等, 2020）检验了类型—元素偏好的对应关系。Tondello 与 Nacke 以 252 名参与者验证 Hexad 驱动的个性化游戏化可提升任务绩效，但同时发现用户类型得分与交互后的元素偏好**并不相关**——这提示静态画像的预测力有限。Colomer（2025，待核验）的实时适配层实验进一步发现，"反画像"配置会提升感知难度、降低适配感。

这一脉络的共同局限是：它把干预当作**静态选项**，回答"给谁配什么"，而不回答"多个干预同时在场时会怎样"。

### 2.2 行为改变科学与 JITAI

JITAI 文献提供了另一条思路：干预应当在"脆弱时刻"被即时投递。Hsu 等（2024）的 62 项研究范围综述指出，多数 JITAI 依赖自陈数据适配，且报告透明性不足、有效性异质。数字时代行为改变的理论整合工作（Zhang 等, 2021）更明确警告：**当多个干预技术针对同一过程或构念时，其组合未必优于各成分之和**。这为本文所考察的类型 I 冲突提供了来自行为改变科学的独立佐证：文献早已承认该现象，却从未把它形式化为可自动检测、可在代码层复核的冲突类型。

### 2.3 多目标辨识与预算约束在线学习

多目标 bandit 的 Pareto 集辨识（Kone 等, 2024；2025）在固定预算/固定置信度下给出算法与信息论下界，后者进一步处理线性可行性约束；MORL 的政策组合（Kim 等, 2025）以 α-近似覆盖广义 p-均值偏好，C-MORL（Liu 等, 2025）以约束优化序列发现完整 Pareto 前沿且复杂度随目标数线性增长；预算约束 MAB（Burnetas 等, 2025）给出 side-constraint 情形的渐近最优策略；Reymond 等（2024, AAMAS）把"拉臂预算"与"查询预算"分离，与本文"干预预算 vs 用户打扰成本"的张力高度同构；字典序 Lipschitz bandit（Xue 等, 2025, JMLR）给出按重要度分层的遗憾界，正是本文所考察的合规抢占语义的数学化。

需要说明这些工具在本文中的角色变化。在原设计阶段，它们被当作待实现的分配模型骨架；在本文的审计结论下，它们退为**背景**——因为实测显示，类型 II 在当前系统中运行时竞争对数为 0，分配问题尚无现实载体。工具仍在，问题却暂时不存在，这一落差本身就是本文负结果的一部分。

### 2.4 游戏化效果基线与暗面

Dai 等（2025）的元分析（182 效应量 / 37 RCT，d = 0.566）与 Slamet 与 Meng（2025）的协同学习元分析（d ≈ 0.875）确立了游戏化的平均效应，但其比较单元是"打包 vs 无游戏化"，恰好暴露了单机制识别与机制间交互的空白。暗面证据则来自 Nafchi（2025）关于数字疲劳与习惯化的系统综述、Xu（2025）在 189 名用户上"仅约 25% 会使用防成瘾功能、75% 选择忽略或绕过"的观察，以及自我决定论关于过度合理化的论述。本文据此坚持"不宣称任何机制对学习有效"，把研究目标锁定在冲突结构的审计。

### 2.5 本章小结

1. 用户类型学脉络回答"给谁配什么元素"，不回答多个干预同时在场时的相互作用。
2. JITAI 文献与行为改变的理论整合工作已承认：当多个干预技术针对同一构念时，其组合未必优于各成分之和；但该现象从未被形式化为可自动检测、可在代码层复核的冲突类型。
3. 多目标辨识与预算约束在线学习工具在本轮审计后退为**背景**：类型 II 在当前系统中运行时竞争对数为 0，分配问题尚无现实载体。
4. 游戏化效果元分析确立了平均效应，但其比较单元是"打包 vs 无游戏化"，恰好暴露了单机制识别与机制间交互的空白。

---

## 3 审计对象与方法：干预的形式化与冲突检出

**本章摘要**：本章给出本文的两项核心工具——干预七元组《trigger, target_construct, direction, channel, cost, side_effect, precedence》与基于方向对立的类型 I 判据——并报告三类冲突的实测检出：不可归类比例为 42/54 = 77.8%，触发本文审计前设定的 > 20% 证伪条件；类型 I 从静态潜在 27 对收敛到运行时 1 对；类型 II 运行时竞争对数为 0。本章并定位到第 2 层方向消解在当前效果生产者集合下是死代码。

### 3.1 干预七元组与本文的审计判据

本文把干预单元定义为七元组

  **I = 《tr, c, δ, ch, b, se, pr》**，

其中 tr 为触发条件（谓词，作用于决策快照）；c ∈ C 为作用构念，C 为构念集合（参与度、坚持性、自我效能、社交联结、在线时长、健康合规）；δ ∈ {+1, −1} 为作用方向（促进／抑制）；ch 为呈现通道；b 为干预预算消耗；se 为已知副作用集合；pr 为合规优先级（整数，越大越优先）。构念集合 C 由三类理论导出——游戏化机制分类学（MDA 与游戏元素分类）、自我决定论（自主、胜任、关系三类基本心理需要）与行为改变技术分类（Fogg 行为模型与 COM-B 的 capability / opportunity / motivation 分解）；方向字段 δ 由行为改变技术的"促进／抑制"语义继承；优先级字段 pr 由合规要求（未成年人保护）而非工程偏好设定。

**判据的预先固化。** 为避免"无法归类本身也是发现"式的自我豁免，本文在审计执行前就把结论判据写死：若不可归类比例低于 5%，说明 schema 的构念集合已足够覆盖，则"schema 粒度不当"这一发现不成立；若高于 20%，则说明 schema 的粒度本身需要重设。第 3.2 节将表明，实测结果落在后者。

### 3.2 映射覆盖率与不可归类比例：实测 77.8%，触发本文自设的证伪条件

审计把 54 个机制逐字段映射到七元组，实测结果如下。

**字段可得性**：七个字段中，只有 `trigger`（实现为 `stage`）在 54 个机制上全部可得（54/54），`direction` 在 12 个机制上可得（12/54，见 3.3.1 节的方向表）。其余四个字段——`target_construct`、`channel`、`side_effect`、`precedence`——**在代码中不存在**：注册表与编排器均未定义这些字段，任何取值都只能由分析者从文档语义中反推，而非从实现中读出。

**不可归类比例**：按"能否被赋予 `direction` 字段"这一 schema 关键字段口径，无法归类的机制为 **42/54 = 77.78%**。42 个无方向标注的机制 ID 清单见 `results/m2/m2_conflict_analysis.json` 的 `4_unclassifiable.no_direction_ids` 字段。

**这个数字触发了本文自设的证伪条件。** 如 3.1 节所述，本文在审计前设定的判据是"不可归类比例高于 20% 即表明 schema 粒度本身需要重设"。实测 77.8% 远超该阈值，因此本文的结论**不是**"实验性 schema 的覆盖率尚可"，而是：**该七元组在当前实现上的粒度必须重设**——一个把"作用构念""呈现通道""副作用""合规优先级"都列为必填字段的形式化，若其中四个字段在实现中完全不存在，那么它描述的是一个设计意图，而不是一个可被审计的系统。这一结论对本文是自我否定性质的，本文不将其软化。

**一个必须并列给出的对照口径**：若改问"无法落入治理文档 A–H 八个类别"的机制占比，实测为 **0%**（治理文档 54 条全部带 A–H 归类）。两个数字并不矛盾，它们测量的是两件事：治理文档的分类是"有归属"的，而本文所考察的七元组是"字段级可得性"的。一个机制被归入 A 类，并不等于它的 `channel` 或 `side_effect` 被实现。本文采用后一口径，因为只有它回答了"系统能否被自动审计"这一问题。

### 3.3 三类冲突的检出

#### 3.3.1 类型 I · 效果方向冲突

类型 I 的冲突条件为：c_i = c_j 且 δ_i = −δ_j 且触发窗口 tr_i ∧ tr_j 可同时为真。审计从三处数据源交叉检查：`mechanism_arbitrator.py` 的方向表、预算策略，以及 `learning_orchestrator.py` 中真实的干预效果生产者。

**方向表覆盖率**：`_DIRECTION` 只登记 **12/54** 个机制（`mechanism_arbitrator.py:102-107`）。其中 approach（促进方向）9 个：`LF-M07, M08, M10, M13, M25, M26, M33, M35, M44`；withdraw（抑制方向）3 个：`LF-M51, M52, M53`。**静态潜在对（上界）**因此为 9 × 3 = **27 对**。

**文档锚定的同构念对**：按治理文档的构念语义，同一作用构念（在线时长）上的实质对为 **2 对**——LF-M44 错失恐惧（approach）↔ LF-M51 强制休息（withdraw），以及 LF-M44 错失恐惧（approach）↔ LF-M52 未成年保护（withdraw）。

**运行时实际可检出**：**1 对**（LF-M44 与 LF-M52）。全仓库只有两处构造干预效果：

| 机制 | 构造位置 | 运行属性 |
|---|---|---|
| LF-M44 | `deep_addiction_engine.py:296` | `user_visible=True, health_critical=False, cost=1.5, priority=50` |
| LF-M52 | `learning_orchestrator.py:986` | `user_visible=False, health_critical=True, cost=0.0, priority=100` |

**静态 27 对与运行时 1 对之间的落差**，是本类型最重要的实测事实：方向表里登记了对立方向，并不意味着它们会在运行时相遇。落差的来源在下一小节展开。

#### 3.3.2 类型 II · 预算竞争

原设计把第二类冲突称为"通道竞争"，理由是"同一呈现通道在同一时间窗被多个干预争用"。审计表明这一命名与实现不符：代码中并不存在通道负荷的度量，存在的是**预算策略** `BudgetPolicy`（`mechanism_arbitrator.py:44-53`），其参数为：`max_per_session = 3`、`max_per_day = 6`、`max_cost_per_session = 4.0`、`min_interval_sec = 300`。因此本文将类型 II **正名为"预算竞争"**；"通道负荷"降为该类型下的一个 remark，而非独立的判定维度。

**实测结果：运行时竞争对数 = 0。** 唯一在运行时可见的非健康候选只有 LF-M44 一个，参与者不足一对；LF-M52 因 `health_critical=True` 直接绕过预算（`mechanism_arbitrator.py:380-382`），不参与竞争。

**零竞争的结构性原因**（这是本节最值得记录的部分）：28 个位于"步骤 13"的次级机制只产出**评估记录**（写入 `decision_snapshot["mechanism_evaluations"]`，见 `learning_orchestrator.py:1105`），它们**不构造干预效果、不进入仲裁器、不消耗预算**。换言之，系统里"登记在册"的机制远多于"真正参与仲裁"的机制，二者之间的差额使预算竞争缺少现实载体。

**由此得出的一句判断：非零 ≠ 可检出。** 即便方向表登记了 3 个抑制方向机制、预算策略也规定了会话与日上限，只要生产者集合把它们挡在仲裁器之外，预算竞争就是一个**在当前系统里尚无可检出载体**的问题。本文认为，这一区分（设计里的冲突 vs 可检出的冲突）应当成为同类审计工作的常规报告项，因为把前者当作后者会系统性地高估系统的治理需求。

#### 3.3.3 类型 III · schema 归类冲突

类型 III 考察的是"同一机制在两套分类体系下归属不一致"这一信息建模层面的冲突。审计对象是治理文档的八个字母（A–H）与注册表 `CATEGORIES` 的五个类别（retention / motivation / selfreg / cognition / health）之间的映射关系。

实测结果：**八个治理字母中有 7 个横跨 ≥2 个代码侧类别**，只有 H（健康护栏与伦理）是单一映射。

| 治理字母 | 落入的代码侧类别 |
|---|---|
| A | motivation, retention |
| B | motivation, retention |
| C | cognition, motivation, selfreg |
| D | motivation, retention, selfreg |
| E | cognition, motivation, retention, selfreg |
| F | motivation, retention |
| G | cognition, motivation |
| H | health（唯一单一映射） |

含义是：**两套分类学并不互为精化关系**。同一机制在治理文档中属 A，在代码注册表中属 `retention`，这不是命名差异，而是"归类冲突"在信息建模层面的真实体现——任何试图用一套编码同时服务治理文档与运行时调度的方案，都必须先解决这两个体系的对齐问题。

### 3.4 一个可直接举证的发现：第 2 层方向消解在当前干预效果生产者集合下是死代码

把类型 I 与类型 II 的结果合起来看，可以得到一个此前未被明确记录、且可直接举证的软件工程事实。

系统设计了两层冲突消解：**第 1 层**是健康一票否决（`mechanism_arbitrator.py:157-170`），**第 2 层**是方向消解。方向消解的实现只在 `user_visible = True` 的干预效果之间比较方向（`mechanism_arbitrator.py:344-349`）。而全仓库仅有的两个效果生产者中，LF-M44 是 `user_visible=True, health_critical=False`，LF-M52 是 `user_visible=False, health_critical=True`。因此：

- LF-M52 **从不进入方向比较**，第 2 层方向消解**不被触发**；
- 二者的语义对立实际由**第 1 层健康一票否决**消解——LF-M52 丢弃全部 approach 方向的干预。

**结论：本系统的结构性对立实现的是"健康否决"语义，而非"方向消解"语义；第 2 层的方向消解在当前干预效果生产者集合下是死代码。** 这一判断不依赖任何主观编码，只依赖两处 `Effect` 构造点的属性与一处比较范围，任何读者都可用两条 `grep` 复核。本文认为这是本次审计中最"硬"的工程发现：它说明一个看似完整的两层仲裁架构，其中一层从未被走到过。

需要强调的是，这一发现**不是**"系统有缺陷"的指控，而是一个关于多干预架构的普遍提醒：**仲裁层的存在不等于仲裁路径的可达**。在把"多干预治理"写进设计文档之前，先确认有没有第二个干预效果生产者在同一决策点上竞争，是更基础的一步。

### 3.5 合规抢占性质 S1 的审计结论

原设计把"合规优先"形式化为一条可机检的系统性质 S1：

> **性质 S1（合规抢占）.** 若在状态 s 下存在激活的合规约束 k，则任何满足 pr_i < pr_k 的干预 i 在 s 下必须静默，即其执行指示变量 x_i(s) = 0；反之 x_k(s) 不受任何低优先级干预影响。

审计对该性质的判定是"**在实现层面被满足，但实现方式比陈述更强、也更粗**"。其理由是：LF-M52 以 `health_critical=True` 与 `priority=100` 注册，健康否决层对其无条件放行并丢弃全部 approach 方向干预（`mechanism_arbitrator.py:157-170`、`380-382`），这在效果上等价于"高优先级合规约束激活时低优先级干预必须静默"。但系统并未把这条约束表述为可机检的时序逻辑，也没有留下可复核的仲裁轨迹（见第 5 节），因此 S1 目前的地位是**一条被实现所蕴含、但未被系统自身表达与验证的系统契约**。

这一结论对原设计是一个降级：S1 的价值本应在于"可被模型检验、也可被反例推翻"，但审计发现，由于第 2 层方向消解是死代码、且仲裁轨迹不落库，S1 在当前实现中**既不可能被有效推翻，也没有独立的检验证据**。本文据此把 S1 从"已评估的方法贡献"下调为"由审计导出的设计需求"。

### 3.6 本章小结

1. 七元组中仅 `trigger`（实现为 `stage`，54/54 可得）与 `direction`（12/54 可得）在实现中存在，`target_construct`、`channel`、`side_effect`、`precedence` 四个字段在代码中不存在；不可归类比例 **42/54 = 77.8%**，触发本文自设的证伪条件，结论为 schema 粒度须重设。
2. 类型 I：静态潜在对 **27**、文档锚定的同构念对 **2**、运行时实际可检出 **1**（LF-M44 与 LF-M52）。
3. 类型 II 正名为"预算竞争"，运行时竞争对数为 **0**；结构性原因是 28 个步骤 13 次级机制只写评估记录，不构造干预效果、不进入仲裁器、不消耗预算。
4. 类型 III：治理文档八个字母中有 **7 个**横跨 ≥2 个代码侧注册表类别，两套分类学不互为精化关系。
5. 两项工程发现：第 2 层方向消解在当前效果生产者集合下是**死代码**；性质 S1 被实现所蕴含、但未被系统自身表达，故从"已评估的方法贡献"下调为"由审计导出的设计需求"。

---

## 4 机制普查、分类学冲突与数字漂移

**本章摘要**：本章报告机制普查与计数口径。治理文档以 A–H 八个类别组织 54 个机制，而代码侧注册表的 `CATEGORIES` 只有五个取值；两组分布之和均为 54，但不能互相印证。本章并给出接线强度分层、四个维度的机制普查分布，以及资产与计数指标相对登记基线的漂移，并披露 PRD 标题声称 76 与其 §2.3 表格求和 69 自相矛盾。

### 4.1 两套分类学：治理文档的 8 个类别与代码侧的 5 个类别

**首先必须澄清一处极易误写的事实**：治理文档以 A–H **八个类别**组织机制。而代码侧注册表的 `CATEGORIES` 常量只有**五个**取值。审计中未发现任何把机制按 A–H 归类的位置；八个类别只能从 Markdown 治理文档解析得到。因此，任何把 A–H 类别体系归于实现代码的表述都是不实的。

治理文档的八个类别及其机制数为：

| 类别 | 标题 | 机制数 |
|---|---|---:|
| A | 行为主义·强化与奖励 | 8 |
| B | 承诺、损失与目标梯度 | 8 |
| C | 自我决定论：自主·胜任·关联 | 6 |
| D | 社会影响与社会学习 | 10 |
| E | 习惯形成与自我调节 | 10 |
| F | 情绪与动机触发 | 4 |
| G | UX 微交互与认知负荷 | 4 |
| H | 健康护栏与伦理 | 4 |
| | **合计** | **54** |

代码侧注册表的五个类别及其机制数则为：retention 19 / motivation 18 / selfreg 9 / cognition 4 / health 4，合计 54。两组分布之和都等于 54，但**不能互相印证**——它们是两套独立分类学对同一批机制的两次归类，其关系已在 3.3.3 节被量化为"7/8 个治理字母横跨 ≥2 个代码侧类别"。

本文正文凡提及"8 个类别"，一律指治理文档的分类学，与代码侧类别体系分属两套体系。

### 4.2 接线强度分层

审计按接线强度对 54 个机制分层，结果如下（口径依据 `scan_mechanism_landing.py:177` 与 `learning_orchestrator.PIPELINE_MECHANISM_MAP`）：

| 分层 | 实测 | 口径依据 |
|---|---:|---|
| `landed`（实现引用可达，或经编排器接线/门控） | **54** | `scan_mechanism_landing.py:177` |
| `orphan` | **0** | — |
| 进入 `PIPELINE_MECHANISM_MAP` 步骤映射 | **34** | 步骤 6(1)+8(2)+9(2)+12(1)+13(28) |
| 仅源码文本引用（未进步骤映射） | **20** | 54 − 34 |
| 其中 `is_enabled("<key>")` 独立门控并真实调用 | **31** | `learning_orchestrator.py` 中 31 个字面量 |
| 3 个健康护栏按设计豁免门控 | 3 | `lai_downgrade` / `forced_rest` / `minor_protection` |

未进步骤映射的 20 个机制 ID 为：`LF-M02, M03, M07, M09, M10, M11, M17, M23, M25, M26, M27, M28, M30, M31, M36, M40, M42, M45, M47, M54`。

**关于"19 还是 20"的口径说明（必须写明分母）**：治理文档的表述是"LF-M01–LF-M53 中 34 个具门控、19 个仅源码文本引用；LF-M54（`class_pet`）为独立 API 模块、不进入门控映射"，即 34 + 19 + 1 = 54，该表述实测成立；若以**全部 54 个机制**为分母，则未进步骤映射为 **20**（34 + 20 = 54）。两个数字都可复算，差异仅在是否把 LF-M54 计入分母。**本文全篇固定采用"全部 54 个机制"为分母，即 20**，并在此标注。

### 4.3 机制普查（Table 1 素材）

审计对 54 个机制作了逐项普查，四个维度的分布如下。

| 维度 | 分布 |
|---|---|
| stage（触发阶段） | during 20 / pre 14 / ambient 13 / post 7 |
| maturity（成熟度） | complete 9 / partial 37 / placeholder 8 |
| disposition（处置） | R 32 / K 15 / M 6 / D 1 |
| direction（方向） | unassigned 42 / approach 9 / withdraw 3 |

这张表本身就是 3.2 节结论的旁证：`direction` 维度上 42 个机制处于 unassigned，与 77.8% 的不可归类比例同源；`maturity` 维度上仅 9 个机制为 complete，说明"登记在册"与"实现完备"之间存在系统性差距。

### 4.4 数字漂移与 PRD 自相矛盾

审计复算了资产与计数指标，结果与登记基线存在可复算的漂移。

| 指标 | 登记基线 | 实测 | 判定 |
|---|---:|---:|---|
| `python_loc` | 25,389 | **25,795** | 漂移 +406 |
| `source_files` | 87 | **88** | 漂移 +1 |
| `service_modules` | 54 | **55** | 漂移 +1 |
| `api_files` | 13 | 13 | 一致 |
| `api_routes_defined` | — | 104 | — |
| `api_routes_reachable` | 104 | 104 | 一致（unreachable = 0） |
| `test_files` | 53 | **54** | 漂移 +1 |
| `test_functions` | 826 | **840** | 漂移 +14 |
| `tests_collected`（pytest 实收） | 920 | **934** | 漂移 +14 |

> **复算口径与透明注记**：本表「实测」列为对当前工作树运行 `scripts/verify_asset_numbers.py --with-pytest`（零依赖：AST 静态解析 + `pytest --collect-only`，不导入 app）所得，相对「登记基线」存在前进漂移（`tests_collected` 920→934、`python_loc` 25,389→25,795 等），源于正常开发。须澄清：`artifacts/count_verification.json` 并非审计时点封版物，与 `asset_numbers.json` 同批（13:40）生成；且 `git` HEAD 仅记录提交 8a328430，不捕获彼时 13 项未提交改动，故「checkout 8a328430 即可复现上表旧值」不成立。本表数值以本次复算为准，可一键复现（`python scripts/verify_asset_numbers.py --with-pytest`）。另需如实说明：`python_loc` 增量的一部分来自此后落地的难度融合估计器（`app/services/difficulty_fusion.py::estimate_optimized_difficulty`，随提交序列 `4073bb0` 及其后改动），故本表漂移值大于审计时点的快照值；该算法侧改动不改变本文的任何审计结论。

计数层（六层口径复算）的实测结果为：`engine_classes`（L0）**86**、`mechanism_units`（L1）**60**、`mechanism_unique`（L2）**54**、`learning_methods` **28**、`skill_tree_nodes` **16**、`prd_claimed` **69**。其中 L2 的 54、28 种学习方法与 16 个技能树节点为严格口径（`strict`）验证通过。

**成熟度口径的另一处文档漂移**：治理文档记为"9 完整 / 41 部分 / 4 占位"，而实测为 **9 / 37 / 8**。本文采用实测值。

**PRD 自相矛盾作为"多口径无约束"的证据**：产品需求文档 §2.3 的表格逐项求和为 **69**，而同文档的标题与概述写作 **76**，两者不一致（`internally_inconsistent = true`）。需要说明，本文不对 76 这一数字作任何有效性主张：它既不是脚本输出，也不是某一层级的口径，而是一处人工填写且与同文档表格冲突的声称。本文全篇严格采用 **54** 这一机制口径（L2，语义去重后唯一机制）；76 仅在上述"主动披露文档自相矛盾"的语境中出现，并同时给出 69。这一披露是刻意的：依托系统的公开仓库使被动揭穿的成本极高，而审计过程本身恰说明，正是注册表与指纹机制使多层口径的差异得以被自动定位。

**仓库前进的披露。** 审计完成后，被审计仓库继续前进：最新 HEAD 为 `4073bb0`，并在该提交序列上叠加了难度融合估计器的增量落地。因此上表的资产指标相对审计时点（2026-09-11，`8a328430`）另有新增漂移。本稿所有数字均已按 2026-09-13 的复算值更新，复算命令不变（`python scripts/verify_asset_numbers.py --with-pytest`、`python scripts/verify_counts.py`）。须强调：该前进属正常开发增量，**不影响本文的任何审计结论**——本文的机制计数、冲突检出与跨系统复现结论均由只读审计脚本产生，与资产行数无关。

### 4.5 本章小结

1. A–H 八个类别是**治理文档分类学**，不得归于代码侧注册表；代码侧 `CATEGORIES` 只有 retention / motivation / selfreg / cognition / health 五个取值，两组分布之和均为 54 但不可互相印证。
2. 接线强度分层：`landed` **54**、`orphan` **0**；进入 `PIPELINE_MECHANISM_MAP` 步骤映射 **34**，未进入 **20**（本文统一以全部 54 个机制为分母）。
3. 机制普查四维分布：`stage` during 20 / pre 14 / ambient 13 / post 7；`maturity` complete 9 / partial 37 / placeholder 8；`disposition` R 32 / K 15 / M 6 / D 1；`direction` unassigned 42 / approach 9 / withdraw 3。
4. 计数层六项：`engine_classes` **86**、`mechanism_units` **60**、`mechanism_unique` **54**、`learning_methods` **28**、`skill_tree_nodes` **16**、`prd_claimed` **69**。
5. 资产漂移以 2026-09-13 的复算为准（`python_loc` 25,389→**25,795**、`tests_collected` 920→**934**）；PRD 标题声称 **76** 与同文档 §2.3 表格求和 **69** 自相矛盾，本文全篇严格采用 **54** 这一机制口径。

---

## 5 从审计导出的设计需求：可审计干预账本

**本章摘要**：本章把可审计干预账本从"已评估贡献"降级为"由审计导出的设计需求"。审计确认仓库中不存在账本实现：无 `budget_consumed` 字段、无 `preempted_by` 字段、无 ledger 模块，`ArbitrationTrace` 亦未落库。因此三类一致性检查本轮产不出数字，本章只能给出账本 schema、三类违规的检查定义，以及以 Merkle 锚定替代分布式账本的设计论证。

### 5.1 账本 schema 与当前实现状态

在原设计阶段，可审计干预账本被列为一项"已评估贡献"；审计后本文把它**降级为一项由审计导出的设计需求**，理由是它当前并未实现。理由如下。

仓库中不存在干预账本实现：

```
grep -rln "budget_consumed\|preempted_by\|ledger" app/    # → 无任何匹配
```

具体而言：无 `budget_consumed` 字段、无 `preempted_by` 字段、无 ledger 模块；`ArbitrationTrace` 只在内存或单次调用中存在（`mechanism_arbitrator.py:56-68`），**未落库为只追加账本**。这与项目自身的《期刊论文拆分方案》第 463 行自述一致（"`budget_consumed` 与 `preempted_by` 字段尚未实现，须在实现章节如实标注为待完成项"）。

因此，本文提出的账本 schema——每条记录含 `mechanism_id`、`intervention_instance_id`、`session_id`、`decision_snapshot`（含能力估计、各源难度、融合权重、心流区、机制开关、实验分组）、`budget_consumed`、`arbitration_decision`、`preempted_by`、`timestamp`——目前只能作为**待实现的设计规格**给出。系统已有的 `learning_events` 事件流表实现了 `decision_snapshot` 的冻结与 `record_learning_event` 埋点，可作为账本落库的接入点，但账本本身尚不存在。

### 5.2 三类一致性检查：本轮产不出数字

原设计定义了账本的三类违规及其检查算法：

| 违规类型 | 定义 | 计划检查方式 | 本轮状态 |
|---|---|---|---|
| 预算超支 | 会话内 Σ `budget_consumed` > B | 按 session 聚合比较 | 无法执行（字段不存在） |
| 被抢占却仍执行 | `preempted_by` 非空但 `arbitration_decision = executed` | 记录级断言 | 无法执行（字段不存在） |
| 决策无记录 | 出现干预效果但无对应账本记录 | 事件流与账本对账 | 无法执行（账本不存在） |

**本文不声称已做账本一致性实验，也不报告任何检出率、漏报率或误报率。** 这些数字在当前仓库状态下不可能产生。可以实跑的替代物只有第 4.4 节的资产复算、以及建表脚本可移植性与状态持久化检查：SQLite 下建表脚本 13/13 条语句成功、6/6 张表建出（`user_xp_state, skill_defs, user_skill_tree, experiments, experiment_assignments, experiment_results`）为 PASS；状态持久化检查 9/9 项 PASS（含"模拟进程重启后 `total_xp` 仍为 150"与"`members` 元素还原为 `TeamMember` 实例"）；`impl_ref` 引用完整性 54/54 OK。这些与账本无关，列在此处只为界定"哪些结论有实跑支撑"。

### 5.3 以 Merkle 锚定替代分布式账本（设计论证）

作为设计论证而非实测结论：账本的防篡改需求可由 Merkle 树加周期性公开锚定满足，其成本与复杂度较分布式账本低约两个数量级，且无需任何机构授权或法律承认。本文据此建议把"教育数据可信处理"的合理内核吸收进账本设计，而不引入区块链技术本身。需要再次强调，这是一条**尚未被实现的建议**，其可行性有待账本落地后验证。

### 5.4 本章小结

1. 仓库中不存在干预账本实现，故本章全部内容为**设计规格**，不是实测结果。
2. 三类一致性检查（预算超支、被抢占却仍执行、决策无记录）本轮均**无法执行**，本文不报告任何检出率、漏报率或误报率。
3. 可实跑的替代物仅有建表脚本可移植性（SQLite 下 13/13 条语句成功、6/6 张表建出）与状态持久化（9/9 项 PASS），二者均与账本无关，列出的目的仅在于界定"哪些结论有实跑支撑"。
4. Merkle 树加周期性公开锚定是一条**尚未被实现**的建议，其可行性有待账本落地后验证。

---

## 6 跨系统复现（E2）

**本章摘要**：本章报告跨系统复现（E2）。对 Ludilearn（6 个机制）与 Level Up XP（11 个机制）的逐文件编码显示：严格口径下两个外部系统的类型 I 冲突均为 0。Ludilearn 以互斥激活使同一 section 同一时刻只有一个游戏元素生效；Level Up XP 以 H/D/W/M 四级速率窗口把干预强度事前封顶，其类型 III 为 1。外部系统编码为分析师单编码、未算评分者间信度，故本文对严格口径一律并列给出放宽上界。

跨系统复现的目的是回答一个原设计无法回答的问题：**"多干预冲突"是本系统单点的毛病，还是该类系统的普遍属性？** 审计对两个公开游戏化系统作了浅克隆与逐文件编码。

### 6.1 审计对象与编码方法

- **系统 A · Ludilearn**：`https://github.com/DigiDago/moodle-format_ludilearn`，commit `eb69582fb4def6e4cbb26c2146ceb90435951c53`，GPL-3.0，PHP。该系统同时是 LudiMoodle+ 项目（法国 ANR e-FRAN / France 2030），**明确以"自适应游戏化"为卖点**，且带 Hexad 画像适配算法（`classes/local/adaptation/hexad_scores.php`）。机制清单按 `classes/local/gameelements/*.php` 去除抽象基类与 `nogamified` 枚举，共 **6 个**：`avatar, badge, progress, ranking, score, timer`。
- **系统 B · Level Up XP**：`https://github.com/FMCorz/moodle-block_xp`，commit `65541fdc9c77511a906353f6660e195eeaa51893`，GPL-3.0，PHP。机制清单按 `classes/local/{xp,leaderboard,badge,rule,division,notification,check}` 与 `classes/form/{cheatguard,promo}` 枚举，共 **11 个**。

**编码方法与其局限（必须并列陈述）**：`target_construct`、`direction`、`channel` 三个字段是**分析师单编码**得到的语义标注——外部系统**不自带** `direction` 元数据，每个编码均附 `file:line` 锚点（见 `results/m2/m2_e2_external.json` 的 `schema` 字段）。本文**未计算评分者间信度（IRR）**，因此不宣称双重编码。为暴露编码判据的敏感性，下文在报告"严格类型 I"（要求同 target 且方向相反）的同时，一律给出**放宽上界**（忽略 target 匹配、只要求方向对立）。

### 6.2 Ludilearn：类型 I 与类型 III 均为 0，冲突按构造不存在

| 冲突类型 | 检出 | 说明 |
|---|---:|---|
| 类型 I（严格：同 target 且方向相反） | **0** | `score` 等为 approach；`timer` 为 withdraw（超时扣分，`timer.php:46` 的 `DEFAULT_PENALTIES=20`），但 target 不同（参与度/表现 vs 时长/节奏） |
| 类型 I（放宽上界：任意 approach × withdraw） | 5 | 仅在忽略 target 匹配时成立 |
| 类型 II（预算竞争） | 无预算概念 | 4 个机制（badge/progress/ranking/score）写同一用户状态，但系统无速率上限 |
| 类型 III（目标冲突） | **0** | 无合规/健康护栏机制 |

**关键结构性发现**：Ludilearn 的 6 个元素**互斥激活**——`classes/local/gamification/manager.php:126-146` 的 `attribution_game_element()` 在把某个游戏元素赋给某 section 之前，先 `DELETE` 该 section 下其它元素的 attribution（SQL 条件 `sectionid=... AND type != ...`）。因此即便放宽到任意方向对立，类型 I 冲突也不可能被激活：同一 section 在同一时刻只有一个游戏元素生效。

**复现结论**：本文所考察的多干预冲突问题在 Ludilearn 中**按构造不存在**——它用"互斥分配"回避冲突，而不是用仲裁解决冲突。

### 6.3 Level Up XP：类型 III 为 1，速率窗口与预算治理同构

| 冲突类型 | 检出 | 说明 |
|---|---:|---|
| 类型 I（严格） | **0** | 无同 target 反向对 |
| 类型 I（放宽上界） | 18 | 2 个 withdraw（`rule_limits`、`cheatguard`）× 9 个 approach |
| 类型 II（预算竞争） | **存在，且有显式上限** | 5 个机制（xp_points/levels/leaderboard/rank/badge）写同一用户状态；`classes/local/ruletype/limit_spec.php` 提供 **H/D/W/M 四级速率窗口**加 `timesallowed` 上限 |
| 类型 III（目标冲突） | **1** | `cheatguard`（合规，防作弊拦截）对全部 approach 机制 |

**复现结论**：Level Up XP 是**纯参与度系统**（无健康/福祉护栏），因此本系统第 1 层的"健康否决"语义在该系统中**无对应物**。它的预算治理落在**速率窗口**（`limit_spec`）上，与本系统第 3 层的"会话/日/成本"预算**同构但粒度不同**。这为"干预预算"这一概念提供了跨系统对照：它并非本文臆造——在工业界系统中确实独立出现，并且需要被治理。

### 6.4 跨系统结论：冲突被结构性规避，而非被解决

把两个系统与本系统并排放，得到本文最重要的比较结论：

| | 本系统 | Ludilearn | Level Up XP |
|---|---|---|---|
| 机制数 | 54 | 6 | 11 |
| 类型 I（严格） | 1（且由健康否决消解） | 0 | 0 |
| 类型 I（放宽上界） | 27（静态潜在对） | 5 | 18 |
| 类型 II | 0（无载体） | 无预算概念 | 有显式速率上限 |
| 类型 III | 7/8 字母跨类 | 0 | 1 |
| 冲突处理方式 | 分层仲裁（第 1 层被走到，第 2 层为死代码） | 互斥激活，按构造不可能 | 速率窗口，事前封顶 |

**结论句**：在这两个外部系统里，冲突不是被解决了，而是**被结构性规避了**——Ludilearn 用互斥激活让对立元素不可能同时在场，Level Up XP 用四级速率窗口把干预强度事前封顶。反过来说，**正是"54 个机制同时登记在册"这一架构，才让冲突成为可观察对象**。因此，本文的问题定位应当收敛为一个更窄、也更诚实的表述：多干预并存的架构是让干预冲突暴露的**稀缺条件**，而不是该类系统的普遍困境。

这一结论对原设计的"跨系统复现会把单点观察升级为类发现"的预期是一个**反例**：复现确实完成了，但它支持的是"冲突稀少"而非"冲突普遍"。本文如实报告这一方向性反转。

### 6.5 本章小结

1. 严格口径下两个外部系统的类型 I 冲突均为 **0**（Ludilearn 0、Level Up XP 0）。
2. Ludilearn 的冲突**按构造不存在**：`attribution_game_element()` 在赋值前先删除同 section 下其它元素的 attribution（`classes/local/gamification/manager.php:126-146`）。
3. Level Up XP 为纯参与度系统，无健康护栏，其类型 III 为 **1**（`cheatguard` 对全部 approach 机制）；预算治理落在 `limit_spec` 的 H/D/W/M 四级速率窗口上，与本系统的会话/日/成本预算同构但粒度不同。
4. 冲突在两个外部系统中是**被结构性规避**的，而非被解决的；这反向支持"多干预并存架构是冲突暴露的稀缺条件"这一结论。
5. 方法学限制：`target_construct` / `direction` / `channel` 为单编码、未算 IRR，故严格判定高度依赖编码判据，本文一律同时给出放宽上界（Ludilearn 5、Level Up XP 18）。

---

## 7 预测与实测对照

**本章摘要**：本章把审计前固化的五项预测与实测结果并排呈现。P1（不可归类比例预期 10–20%）被实测 77.8% 推翻并触发证伪条件；P2（三类冲突普遍存在）被推翻；P3（跨系统复现将把单点观察升级为类发现）方向反转，同样被推翻；P4（S1 可通过模型检验）与 P5（账本三类违规可被可靠检出）因被审计系统的实现缺口而未能检验。凡预测被推翻者，一律保留原预测原文方向，不做事后修饰。

本节把审计前固化的各项预测与实测结果并排呈现。凡预测被推翻者，一律保留原预测原文方向，不做事后修饰。

| 编号 | 审计前的预测 | 实测 | 判定 |
|---|---|---|---|
| P1 | 54 个机制映射到七元组后，**预期 10–20% 因语义模糊无法归类**；判据：> 20% 则 schema 粒度须重设 | **77.8%（42/54）**，四个字段在代码中不存在 | **预测被推翻 → 触发证伪条件**，结论为 schema 粒度须重设 |
| P2 | 三类冲突在本系统普遍存在；类型 I 至少含"FOMO × 未成年保护 × 休息提醒"这一结构性案例 | 类型 I 运行时**仅 1 对**（M44–M52）；类型 II **0 对**；类型 III 为 7/8 字母跨类 | **预测被推翻**（冲突不普遍） |
| P3 | 跨系统复现将把单点观察升级为类发现：两个外部系统亦检出非零冲突 | Ludilearn 类型 I/III 均 **0**；Level Up XP 类型 I **0**、类型 III **1** | **预测被推翻**（方向反转：冲突稀少） |
| P4 | 合规抢占性质 S1 可通过时序逻辑表述并在编排器状态机上**通过模型检验** | S1 在实现层面被满足，但**未被系统自身表达**、无仲裁轨迹落库，因而既不能被有效推翻也无独立检验证据 | **未能检验**（降级为设计需求） |
| P5 | 账本三类违规可被可靠检出（预期检出率 > 0.95、误报率 < 0.05） | **无法执行**：`budget_consumed`/`preempted_by`/ledger 在仓库中零匹配 | **未检验**（原假设删除） |

三处被推翻的预测（P1–P3）都不是"实验做坏了"，而是审计前对系统的判断本身过于乐观；两处未能检验（P4–P5）则暴露了被审计系统的实现缺口。本文认为，把这五行完整报告出来，比只报告支持性结果更有价值——它把"预测先于数据"这一约束真正兑现成了可核对的形式。

### 7.1 本章小结

1. P1–P3 三处预测被推翻，方向均与审计前的预期相反；其中 P1 直接触发本文自设的证伪条件。
2. P4、P5 两处未能检验，暴露的是被审计系统的实现缺口：S1 未被系统自身表达，账本与 `budget_consumed` / `preempted_by` 字段均不存在。
3. 三处被推翻的预测不是"实验做坏了"，而是审计前对系统的判断本身过于乐观；本文不将其软化，也不事后修饰预测方向。
4. 完整报告被推翻与未检验的预测，是"预测先于数据"这一约束的可核对形式。

---

## 8 讨论

**本章摘要**：本章讨论本文的定位与实践含义。审计结论迫使本文对自身定位作一次下调：S1 被实现所蕴含但未被系统自身表达、分配模型因运行时竞争对数为 0 而无现实载体、跨系统复现完成但方向与预期相反。因此本文不宣称提出了一个已被验证的方法，其贡献是审计本身。对系统设计者，本章给出三条可直接操作的启示。

### 8.1 对"这是分类学还是方法"的自我评估

审计结论迫使本文对自身的定位作一次诚实下调。原设计为跨越"这只是一份分类学"的判词准备了三条件：S1 的可机检性、分配模型的遗憾分析适用前提、E2 的跨项目复现。实测的结果是：S1 被实现所蕴含但未被表达（3.5 节），分配模型的适用前提因**运行时竞争对数为 0** 而无现实载体（3.3.2 节），E2 的复现完成了但方向反转（6.4 节）。因此，**本文不宣称自己提出了一个已被验证的方法**；本文的贡献是审计本身——它把一个此前被假定存在、却从未被检查的问题（多干预运行时冲突）查清为"在本系统与两个外部系统中均几乎不发生，且不发生的原因是结构性规避"。

在实证软件工程的语境里，这一结论的价值不低：它把一个广泛流传的设计直觉（"机制越多，冲突越多，越需要仲裁与预算"）替换为一个有代码锚点的经验事实，并且指出了该直觉会在什么条件下才成立（干预效果生产者集合中有两个以上在同一决策点竞争）。

### 8.2 教学与实践启示

对自适应学习系统的设计者，本文的启示有三条，且都可直接操作。第一，**先清点生产者，再谈仲裁**：在声称系统具备冲突治理之前，先 `grep` 干预效果的构造点数量；若同一决策点上只有一个生产者，检索到的"冲突"只存在于文档语义里。第二，**把分类学的对齐纳入设计评审**：治理文档的分类与运行时注册表的分类若不是精化关系（本文实测 7/8 个治理字母横跨 ≥2 个注册表类别），任何依赖单一编码的统计都会失真。第三，**让约束可被系统自身表达**：健康否决当前是一条被实现蕴含、却无法被系统自身检验的契约；把它写成可机检的断言，并让仲裁轨迹落库，是从"设计约定"走向"可验证系统性质"的必要一步。

### 8.3 本章小结

1. 本文**不宣称**自己提出了一个已被验证的方法；贡献是审计本身，即把一个被假定存在、却从未被检查的问题查清为"在本系统与两个外部系统中均几乎不发生"。
2. 设计直觉（"机制越多、冲突越多、越需要仲裁与预算"）被替换为一个有代码锚点的经验事实，并给出该直觉成立的条件：干预效果生产者集合中有两个以上在同一决策点竞争。
3. 三条实践启示：先清点干预效果生产者再谈仲裁；把两套分类学的对齐纳入设计评审；让合规约束成为可被系统自身检验的断言并令仲裁轨迹落库。
4. 上述下调的三项依据分别为 8.1 节的自我评估、3.3.2 节的零竞争实测与 6.4 节的方向反转。

---

## 9 诚实缺口清单（局限）

**本章摘要**：本章把本文的局限与诚实缺口逐条单列，不作合并或弱化。九项缺口分别来自三个方面：环境与样本（MySQL 未实测、跨系统样本仅 2 个且同源、外部编码未算信度、无真人实验）、被审计系统的实现状态（账本未实现、无真实运行时日志）、以及本文自身的定位与尚未完成的工作（不宣称机制有效、LAI 量表未验证、分类学完备性未获证明）。

**① MySQL 建表未实测。** 审计环境无 `sqlglot`、无 MySQL 服务器，MySQL 脚本**未实测**，本文不得声称 MySQL 已验证。可写的只有 SQLite 结果：13/13 条语句成功、6/6 张表建出（PASS）。

**② 跨系统样本量小且同源。** E2 仅覆盖 2 个外部系统，且均为 Moodle/PHP 生态，语言与架构同源，外推性有限。原设计写 2–3 个系统，实际完成 2 个。

**③ 外部系统编码为单编码，未算信度。** `target_construct` / `direction` / `channel` 为分析师单编码，每个编码附 `file:line` 锚点，但**未计算评分者间信度（IRR）**，本文不宣称双重编码。且两个外部系统均无 `target_construct` 字典，"严格类型 I"的检出高度依赖编码判据，故本文一律同时给出放宽上界（Ludilearn 5、Level Up XP 18）以暴露判据敏感性。

**④ 未做任何需人类被试的实验。** 本文不含真人实验，不涉及招募与伦理审查；相应地，本文也不报告任何干预负荷感知、骚扰感或可用性量表结果。

**⑤ 未声称任何机制对学习有效。** 本文不评价游戏化机制的学习效果，也不作单机制消融。所有关于机制的陈述都限定在"实现层面的接线、方向与预算属性"。

**⑥ 账本整章为设计需求而非实测结果。** `budget_consumed` 与 `preempted_by` 未实现，三类一致性检查本轮产不出数字，§5 的账本 schema 与 Merkle 论证均为未实现的设计规格。

**⑦ 学习成瘾测量未验证（仅在作为治理变量被引用时）。** 本文把"健康合规"作为构念集合中的一个受治理目标，其可测量侧面之一为学习成瘾倾向。若引入自陈量表，本文所采用的是学习成瘾指数（LAI）：五维权重为时间 30% / 动机 25% / 控制 25% / 认知 10% / 功能 10%，其中"控制 + 功能"合计 35% 由自陈测量层供给（`instrument_catalog.py` 中的 SRL-STOP / SLEEP-IMPACT / SOCIAL-IMPACT / TIME-BIAS）；未被测量的维度不计入加权并输出 coverage 予以披露，绝不以"健康"静默补分。该量表题项为**本项目自编，未做信效度验证、无常模、无临床截断值**，属开发中的测量工具（work-in-progress instrument），不得据此作临床或诊断性推断。缓解路径为三层并进：①以公开已验证量表（BStAS 5 题，Atroszko 等, 2025；SI-10，Loscalzo 等, 2024；中文青少年学习倦怠量表 ASBI，吴艳等, 2010）作聚合效度锚点；②完成最小信效度集（α/ω + EFA→CFA + 2 周重测 ICC + 与倦怠/焦虑相关）；③以自陈分数与行为日志（夜间作答比例、连续使用天数、退出率）做收敛效度。上述工作均未完成。

**⑧ 静态审计不能替代运行时观测。** 本文的结论来自代码静态审计与跨系统编码，研究依托系统**尚无真实用户产生的日志**，因此无法展示真实干预流下的冲突频率与预算消耗分布。"预算耗竭""被抢占却执行"这类违规在真实运行中到底多常见，本文完全无法回答。

**⑨ 分类学完备性未获证明。** 本文论证了七元组每个字段有理论出处，但未证明字段集合的完备性——这是任何形式化工作的共同局限；且如 3.2 节所示，该 schema 在当前实现上的可得性本身就不足。

### 9.1 本章小结

1. ①–⑨ 九项局限中，②③ 来自样本与方法，①⑥⑧ 来自实现与环境缺口，④⑤⑦⑨ 来自本文的自我限定与尚未完成的工作。
2. 最影响外推性的两项：跨系统样本量小且同源、外部系统编码未计算评分者间信度。
3. 最影响结论强度的两项：静态审计缺乏真实运行时日志支撑、账本未实现使三类一致性检查无从执行。
4. LAI 为项目自编、未做信效度验证的开发中测量工具，不得据此作临床或诊断性推断。

---

## 10 结论

**本章摘要**：本章总结全文。审计的三个核心数字是：不可归类比例 77.8%（触发本文自设的证伪条件，结论为 schema 粒度须重设）；类型 I 冲突从静态潜在 27 对收敛到运行时 1 对，且该对由健康否决层而非方向消解层处理；类型 II 的运行时竞争对数为 0。跨系统复现进一步显示两个外部系统的类型 I 冲突均为 0。由此得到的主线结论是一个负结果。本章并给出三条设计需求与两项后续工作。

本文把一个被广泛假定的设计直觉——"同时运行数十个干预机制的系统必然发生干预冲突，因而需要仲裁与预算治理"——改写成了一个可证伪的经验命题，并给出了证伪它的实测证据。审计的三个核心数字是：不可归类比例 **77.8%**（触发本文自设的 > 20% 证伪条件，结论为 schema 粒度须重设）；类型 I 冲突从静态潜在 27 对收敛到运行时 **1 对**，且该对由第 1 层健康否决而非第 2 层方向消解处理，后者在当前效果生产者集合下是**死代码**；类型 II 预算竞争的运行时竞争对数为 **0**，因为次级机制只写评估记录、不进入仲裁器。跨系统复现进一步显示，两个外部游戏化系统在严格口径下的类型 I 冲突均为 0——它们用互斥激活与速率窗口把冲突**按构造规避**。

由此得到的主线结论是一个负结果：**多干预并存的架构是让干预冲突暴露的稀缺条件，而不是该类系统的普遍困境。** 本文同时报告了自身方法的失败之处（schema 覆盖不足、账本未实现、外部编码未算信度、分类学双轨），并把这些失败转化为三条可操作的设计需求：先清点干预效果生产者再谈仲裁；把两套分类学的对齐纳入设计评审；让合规约束成为可被系统自身检验的断言并令仲裁轨迹落库。后续工作有两项：其一，实现干预账本与 `budget_consumed` / `preempted_by` 字段，使三类一致性检查首次可跑；其二，扩充外部系统样本并引入双人独立编码与信度报告，检验"结构性规避"这一结论能否推广到 Moodle/PHP 生态之外。

### 10.1 本章小结

1. 主线结论为负结果：**多干预并存的架构是让干预冲突暴露的稀缺条件，而不是该类系统的普遍困境。**
2. 三个核心数字分别对应三个判断：77.8%（schema 粒度须重设）、27→1（类型 I 的静态与运行时落差）、0（类型 II 无现实载体）。
3. 三项可操作的设计需求：先清点干预效果生产者再谈仲裁；把两套分类学的对齐纳入设计评审；让合规约束成为可被系统自身检验的断言并令仲裁轨迹落库。
4. 两项后续工作：实现干预账本与 `budget_consumed` / `preempted_by` 字段；扩充外部系统样本并引入双人独立编码与信度报告。

---

## 参考文献

[1] Hsu, T.-C. C., Whelan, P., Gandrup, J., Armitage, C. J., Cordingley, L., & McBeth, J. (2024). Personalized interventions for behaviour change: A scoping review of just-in-time adaptive interventions. *British Journal of Health Psychology*, 30, e12766. DOI: 10.1111/bjhp.12766.

[2] Kone, C., Kaufmann, E., & Richert, L. (2024). Bandit Pareto set identification: The fixed budget setting. *Proceedings of the 27th International Conference on Artificial Intelligence and Statistics (AISTATS)*. arXiv:2311.03992.

[3] Kone, C., Kaufmann, E., & Richert, L. (2025). Constrained Pareto set identification with bandit feedback. *Proceedings of the 42nd International Conference on Machine Learning (ICML)*. arXiv:2506.08127.

[4] Kim, C. W., Moondra, J., Verma, S., Pollack, M., Kong, L., Tambe, M., & Gupta, S. (2025). Navigating the social welfare frontier: Portfolios for multi-objective reinforcement learning. *Proceedings of the 42nd International Conference on Machine Learning (ICML)*, 30631–30653. arXiv:2502.09724.

[5] Liu, R., Pan, Y., Xu, L., Song, L., You, P., Chen, Y., & Bian, J. (2025). Efficient discovery of Pareto front for multi-objective reinforcement learning. *International Conference on Learning Representations (ICLR)*. （条目待核验）

[6] Burnetas, A. N., Kanavetas, O., & Katehakis, M. N. (2025). Optimal data driven resource allocation under multi-armed bandit observations. *Annals of Operations Research*. DOI: 10.1007/s10479-025-06554-3.

[7] Reymond, M., Bargiacchi, E., Roijers, D. M., & Nowé, A. (2024). Interactively learning the user's utility for best-arm identification in multi-objective multi-armed bandits. *Proceedings of the 23rd International Conference on Autonomous Agents and Multiagent Systems (AAMAS)*.

[8] Xue, B., Cheng, J., Liu, F., Wang, Y., Zhang, L., & Zhang, Q. (2025). Lexicographic Lipschitz bandits: New algorithms and a lower bound. *Journal of Machine Learning Research*, 26, 1–56.

[9] Dai, W.-A., Xu, W., & Xing, Q.-W. (2025). Gamified learning impact: A meta-analysis of game element combinations on students' learning outcomes. *Educational Technology Research and Development*, 73(4), 2617–2643. DOI: 10.1007/s11423-025-10493-y.

[10] Slamet, T. I., & Meng, C. (2025). Gamification in collaborative learning: Synthesizing evidence through meta-analysis. *Journal of Computers in Education*. DOI: 10.1007/s40692-024-00349-4.

[11] Nafchi, M. Z. (2025). Digital fatigue and gamification: A systematic literature review. *ACC Journal*. DOI: 10.2478/acc-2025-0010.

[12] Colomer Ferrer, S. (2025). Adapting games to player taxonomy. *2n Simposi sobre Investigació a la Indústria Indie del Videojoc (I3V 2025)*. （条目待核验）

[13] Xu, J. (2025). Digital health tools and habit formation: Investigating the role of anti-addiction systems in mitigating social media dependency. *International Journal of Innovative Research and Scientific Studies*, 8(2), 3332–3339. DOI: 10.53894/ijirss.v8i2.6007.

[14] Zhang, C., et al. (2021). Theory integration for lifestyle behavior change in the digital age: An adaptive decision-making framework. *Journal of Medical Internet Research*. PMC8065564. （作者与卷期待核验）

[15] Atroszko, P. A., Charzyńska, E., Buźniak, A., Czerwiński, S. K., Griffiths, M. D., et al. (2025). Validity, reliability, and cross-cultural comparability of a problematic overstudying scale. *International Journal of Mental Health and Addiction*. DOI: 10.1007/s11469-023-01128-5.

[16] Loscalzo, Y., Wetstone, H., Schuldberg, D., Giannini, M., & Rice, K. G. (2024). Studyholism in the United States and Italy. *Current Psychology*, 43(29), 24608–24621. DOI: 10.1007/s12144-024-06163-6.

[17] 吴艳, 戴晓阳, 温忠麟, 崔汉卿. (2010). 青少年学习倦怠量表的编制. *中国临床心理学杂志*, 18(2), 152–154.

[18] Sailer, M., Hense, J. U., Mayr, S. K., & Mandl, H. (2017). How gamification motivates: An experimental study of the effects of specific game design elements on psychological need satisfaction. *Computers in Human Behavior*, 69, 371–380. （卷期与 DOI 待核验）

[19] DigiDago. (2025). *moodle-format_ludilearn* [Computer software]. GitHub. commit eb69582fb4def6e4cbb26c2146ceb90435951c53. https://github.com/DigiDago/moodle-format_ludilearn （GPL-3.0）

[20] Frédéric Massart (FMCorz). (2025). *moodle-block_xp* [Computer software]. GitHub. commit 65541fdc9c77511a906353f6660e195eeaa51893. https://github.com/FMCorz/moodle-block_xp （GPL-3.0）

### 待核验引用清单

按本稿体例，引用统一为 `[@citekey]` 形式，且键必须真实存在于 `E:\learnflow\docs\references.bib`。本文参考文献共 20 条：其中 17 条在该 bib 中没有对应条目，按体例以 `[@待核验:作者年份]` 形式登记，投稿前必须闭环；2 条为被审计外部系统的软件条目（保留 URL、commit 与许可）；1 条已在 bib 中。逐条如下（编号与上文「参考文献」一致）。

| # | 条目 | 登记形式 |
|---|---|---|
| 1 | Hsu 等 (2024) JITAI 范围综述 | `[@待核验:Hsu2024]` |
| 2 | Kone 等 (2024) Bandit Pareto set identification | `[@待核验:Kone2024]` |
| 3 | Kone 等 (2025) Constrained Pareto set identification | `[@待核验:Kone2025]` |
| 4 | Kim 等 (2025) MORL portfolios | `[@待核验:Kim2025]` |
| 5 | Liu 等 (2025) Pareto front discovery | `[@待核验:Liu2025]` |
| 6 | Burnetas 等 (2025) Resource allocation under MAB | `[@待核验:Burnetas2025]` |
| 7 | Reymond 等 (2024) Best-arm identification in MOMAB | `[@待核验:Reymond2024]` |
| 8 | Xue 等 (2025) Lexicographic Lipschitz bandits | `[@待核验:Xue2025]` |
| 9 | Dai, Xu 与 Xing (2025) 游戏化学习元分析 | `[@待核验:Dai2025]` |
| 10 | Slamet 与 Meng (2025) 协作学习元分析 | `[@待核验:Slamet2025]` |
| 11 | Nafchi (2025) 数字疲劳系统综述 | `[@待核验:Nafchi2025]` |
| 12 | Colomer Ferrer (2025) 玩家类型适配 | `[@待核验:Colomer2025]` |
| 13 | Xu (2025) 防成瘾系统与习惯形成 | `[@待核验:Xu2025]` |
| 14 | Zhang 等 (2021) 数字时代行为改变理论整合 | `[@待核验:Zhang2021]` |
| 15 | Atroszko 等 (2025) 过度学习量表效度 | `[@待核验:Atroszko2025]` |
| 16 | Loscalzo 等 (2024) Studyholism | `[@待核验:Loscalzo2024]` |
| 17 | 吴艳等 (2010) 青少年学习倦怠量表 | `[@待核验:吴艳2010]` |
| 18 | Sailer 等 (2017) 游戏元素与心理需要 | 已在 bib 中，键 `sailer2017how` |
| 19 | DigiDago (2025) `moodle-format_ludilearn` | 软件条目，保留 commit 与许可，不入 bib |
| 20 | FMCorz (2025) `moodle-block_xp` | 软件条目，保留 commit 与许可，不入 bib |

---

## 结果可复算清单

| # | 核验项 | 类型 | 处置 |
|---|---|---|---|
| 1 | 全篇机制计数严格为 54（LF-M01…LF-M54，连续无缺口），指纹 `ee1a49be5732`；学习方法 28、技能树节点 16 不变 | **诚信红线** | 逐处复核；可用 `verify_asset_numbers.py --doc-check` |
| 2 | `76` 是否仅出现于"PRD 自相矛盾"语境，且同处给出 69 | **诚信红线** | 逐处复核（见 4.4 节） |
| 3 | 全文对类别体系的表述是否一律限定为"治理文档分类学"，且未出现任何把 A–H 归类归于代码侧注册表的句子 | **表述红线** | 逐句复核 |
| 4 | 未进步骤映射采用"全部 54 个机制"为分母（= 20），并在 §4.2 标注 | 数字一致性 | 正文与清单各注一处 |
| 5 | L0/L1/L2 三层计数（86/60/54）与 `count_verification.json` 一致 | 数字一致性 | 附可复算脚本 |
| 6 | 资产指标以实测为准（`verify_asset_numbers.py --with-pytest` 复算）：`python_loc` = 25,795（非 25,389）、`tests_collected` = 934（非 920）；成熟度 9/37/8 | 数字一致性 | 复算脚本可一键复现 |
| 7 | §3.2 的 77.8% 与"触发 > 20% 证伪条件"结论是否未被软化 | **结论红线** | 逐句复核 |
| 8 | §7 三处被推翻的预测（P1–P3）与两处未检验（P4–P5）是否完整保留 | 诚信 | 逐行核对 |
| 9 | 账本相关表述是否一律标注"未实现/设计需求"，未出现任何检出率数字 | 诚信红线 | 逐句复核 |
| 10 | 外部系统编码是否标注"单编码、未算 IRR"，并给出放宽上界 | 方法学 | 与 `m2_e2_external.json` 核对 |
| 11 | MySQL 是否标注"未实测"，未声称已验证 | 诚信 | 逐句复核 |
| 12 | LAI 相关表述是否均标注"未验证/探索性" | 诚信红线 | 逐句复核 |
| 13 | 两个外部系统的 commit 号与许可是否与克隆一致 | 引用合规 | 复核 GitHub 页面 |
| 14 | Kone 2024/2025、Kim 2025、Burnetas 2025、Reymond 2024、Xue 2025 的卷期页码 | 引用合规 | CrossRef / OpenAlex 逐条核对 |
| 15 | 《软件学报》字数要求与 AIGC 政策 | 投稿合规 | 以官网为准 |
| 16 | 审计完成后仓库继续前进（最新 HEAD = `4073bb0`，并叠加难度融合估计器增量落地），故资产指标相对审计时点另有新增漂移；本稿数字已全部按 2026-09-13 的复算值更新，复算命令不变（`python scripts/verify_asset_numbers.py --with-pytest`、`python scripts/verify_counts.py`），且该前进**不影响本文任何审计结论** | 诚信披露 | 复算脚本可一键复现 |

---

*本稿为审计型完整稿，由 `M2_游戏化干预冲突检测与仲裁_预注册设计.md`（结果前档案，保留留档、未作任何改动）改写而来。全部数字取自 `results/M2_results.md` 与其原始 JSON 产物，不含估算、不沿用旧稿的设计预期值。数字口径：游戏化机制 54 个（LF-M01…LF-M54）、学习方法 28 种、技能树节点 16 个、注册表指纹 `ee1a49be5732`；"76"仅出现于 PRD 自相矛盾的主动披露语境并同时给出 69。引用均来自真实检索文献，标注"待核验"者投稿前必须闭环。*
